# Экраны и компоненты: кабинет покупателя, кабинет получателя, консоль операций

**Версия:** 0.1 · **Дата:** 2026-08-31 · **Статус:** на приёмку владельцем
**Основание:** `docs/product/PRODUCT.md` 0.2, `docs/product/CABINETS.md` 0.1,
`docs/product/FUNCTIONAL.md` 0.1
**Что это:** спецификация экранов — структура, данные, состояния, поведение,
крайние случаи, адаптивность, компоненты. **Не визуальный стиль.** Дизайн-системы
в проекте пока нет; этот документ — вход для неё и для разработки.

**Чего в документе нет намеренно:**
цвета, шрифты, отступы, иконки, тайминги анимаций — это отдельный проход
(`design-system`, `design-token`, `color-system`, `typography-scale`);
готовые тексты — их пишет связка копирайтер → главред, здесь только ключи и
брифы «что текст должен сообщить»;
бизнес-логика — она в `FUNCTIONAL.md`, здесь только её отражение в интерфейсе.

**Маркеры** (по правилу проекта): **[установлено]** — следует из принятых
документов; **[решение]** — проектное решение этого документа, обратимо;
**[гипотеза]** — требует проверки; **[открыто]** — блокирует, вынесено в §8.

---

## 0. Как читать документ

### 0.1 Нотация вайрфреймов

Макеты — низкая точность, только структура и приоритет. Условные обозначения:

| Запись | Смысл |
|---|---|
| `«ключ.локализации»` | Текстовый слот. **В макете никогда не стоит готовый текст** — только ключ |
| `{данные: путь}` | Значение из данных, форматируется механизмом локали |
| `[Кнопка «ключ»]` | Основное действие |
| `[[Кнопка «ключ»]]` | Единственное основное действие экрана (не более одного) |
| `[ Кнопка «ключ» ]ᵈ` | Действие в состоянии «недоступно», с обязательной причиной рядом |
| `▸ ключ` | Раскрывающийся блок, свёрнут по умолчанию |
| `▣` | Компонент из инвентаря §6, назван по имени |
| `→ S-XX` | Переход к состоянию с кодом |
| `⚠ ключ` | Блокирующее предупреждение, не сворачивается |
| `⏱` | Слот таймера дедлайна |
| `·····` | Строка данных, повторяемая |

### 0.2 Идентификаторы

- Экраны: `B-*` покупатель, `R-*` получатель, `O-*` консоль операций.
- Состояния экрана: `B-03/S-04`.
- Компоненты: `C-**` (§6).
- Состояния «где деньги»: `M-**` (§2.2).

### 0.3 Контракт экрана — три вопроса

Каждый экран во **всех** состояниях, включая пустое, загрузку и ошибку, отвечает:

1. **Где сейчас деньги** — кто фактически держит сумму прямо сейчас.
2. **Что произойдёт дальше** — следующее событие и когда, включая «что случится,
   если никто ничего не сделает».
3. **Что требуется от меня** — ровно одно действие или явное «ничего, ждём X».

**Правило приёмки:** если на экран нельзя показать пальцем и получить три ответа
за пять секунд — экран не принят. Проверяется по чек-листу §7 на каждом
состоянии, а не только на «нормальном».

Это не лозунг, а причина ряда решений ниже: например, почему у пустого состояния
кабинета всё равно есть строка о местонахождении денег, и почему экран ошибки
загрузки говорит «деньги в безопасности, мы не смогли показать страницу», а не
«что-то пошло не так».

### 0.4 Терминология и запреты в интерфейсе

Запреты — не стилистика, а правовая обязанность (`CLAUDE.md`, красные линии;
`CABINETS.md` §0.1).

| Запрещено | Вместо этого | Почему |
|---|---|---|
| «эскроу», «escrow» и любые транслитерации, включая грузинскую | «расчёт по сделке», «резервирование средств» | Эскроу-агент по закону только банк или микробанк |
| «отправить деньги продавцу», «перевести продавцу» | «подтвердить условия расчёта» | Клиент не даёт поручение банку. Поручение даёт платформа |
| «вы перевели», «вы выплатили» | «расчёт исполнен» | То же |
| «ваши деньги у нас», «на нашем счёте» | «на счёте номинального держания в {банк}» | Это не наш баланс, это чужие деньги |
| «мы разблокируем, когда сочтём нужным» | «расчёт исполняется при подтверждённой регистрации» | Условие по воле сторон делает сделку ничтожной |
| «гарантируем», «безотзывно», «заблокировано навсегда» | формулировки через факты и сроки | Безотзывное поручение ничтожно; ложное обещание = потребительский риск |
| «средства заморожены» (о резерве) | «зарезервированы под сделку до ⏱» | Заморозка — санкционный термин, у него другое значение |

Термины, требующие **одного** перевода во всех трёх языках и во всех кабинетах
(глоссарий ведёт главред, ключи — в неймспейсе `glossary.*`): резерв, расчёт,
откат, транш, референс, счёт-источник, номинальное держание, пакет доказательств,
наблюдение, покрытие.

### 0.5 Что финализируется не здесь

Каждый ключ ниже сопровождается **копи-брифом**: что текст обязан сообщить, чего
не имеет права обещать, лимит длины. Финальные формулировки — копирайтер →
главред. Юридически чувствительные слоты помечены **⚖** — они идут через
`legal-compliance-ru`, минуя главреда: карточка подтверждения средств, правила
распределения расходов при срыве, текст о невозвратности спреда, декларация PEP,
формулировка условия расчёта, тексты согласий.

---

## 1. Сквозные правила

### 1.1 Локализация

**[установлено]** Русский, английский, грузинский с первого дня. Консоль
операций — русский и английский, без грузинского.

**Ни одной строки текста в компонентах.** Компонент принимает ключ или уже
разрешённую строку, но никогда не содержит литерал. Это проверяется линтером на
литералы в JSX, а не ревью **[решение]**.

**Схема ключей.** `<область>.<экран>.<блок>.<элемент>[.<состояние>][.<роль>]`

```
buyer.deal.whereMoney.reserved.title
buyer.deal.whereMoney.reserved.body
buyer.topup.payerRule.warning
payee.assurance.outcomes.registered.body
ops.payout.approve.blocked.samePreparer
common.deadline.consequence.rollback
glossary.tranche
```

Области: `buyer`, `payee`, `ops`, `common`, `glossary`, `legal`, `notify`.
Один ключ — один смысл. **Ключи не переиспользуются между областями**, даже если
русский текст совпадает: в грузинском он может не совпасть.

**Плюрализация** — через ICU MessageFormat, все формы каждого языка (ru: one/few/
many/other, en: one/other, ka: one/other + собственные особенности числительных).
Ни одной конкатенации «{n} + слово».

**Числа, даты, валюты, имена** — через `Intl`, не своими функциями. Лари
форматируется по грузинским правилам. **Суммы приходят с сервера в целых минорных
единицах** и форматируются на клиенте; в компонент никогда не передаётся число с
плавающей точкой (красная линия №4).

**Длина строк.** Макеты проверяются на грузинском и русском, не на английском.

| Тип слота | Бюджет EN | Запас на RU | Запас на KA | Поведение при переполнении |
|---|---|---|---|---|
| Метка кнопки | ≤ 20 симв. | ×1.4 | ×1.6 **[гипотеза]** | Перенос на 2 строки, кнопка растёт. **Никогда не обрезается** |
| Заголовок блока | ≤ 40 | ×1.35 | ×1.5 | Перенос |
| Тело блока | ≤ 240 | ×1.35 | ×1.5 | Растёт |
| Ячейка таблицы (текст) | ≤ 32 | ×1.35 | ×1.5 | Обрезка + тултип + полное значение в детали |
| Ячейка таблицы (деньги, дата, статус) | — | — | — | **Не обрезается никогда.** Колонка расширяется, обрезаются соседние |
| Метка поля формы | ≤ 30 | ×1.4 | ×1.6 | Перенос, поле не сдвигается |
| Чип фильтра | ≤ 18 | ×1.4 | ×1.6 | Перенос списка чипов на новую строку |

Коэффициенты — **[гипотеза]**, уточняются после первого прохода реальных
переводов; до этого макеты проверяются на псевдолокали, растягивающей EN на 60%.

**Правило вёрстки:** никаких фиксированных ширин у элементов с текстом. Кнопки,
чипы, бэджи, табы — по контенту с минимальной шириной. Сетки — на `minmax()`.

**Задел под RTL:** логические свойства (`inline-start/end`, `margin-inline`)
вместо физических с первого коммита. Иконки направления зеркалируются, иконки
объектов — нет. Порядок чисел и денежных сумм в RTL не переворачивается.

**Язык выбирается пользователем, не геолокацией**, хранится в профиле,
применяется ко всем каналам, включая письма. Переключатель языка доступен **до
входа** (на экране входа) — иностранец не должен угадывать пароль на незнакомом
языке. ▣ C-21.

**Имена собственные** (стороны, банки, адреса объектов) не переводятся и
показываются в исходном алфавите; в консоли — с транслитерацией второй строкой,
см. §5.4 и `FUNCTIONAL.md` §5.3.

### 1.2 Часовые пояса и дедлайны

Продукт трансграничный: покупатель в другом часовом поясе, дедлайн привязан к
банковскому дню в Тбилиси. Показ дедлайна «до 18:00» без зоны — прямой путь к
пропуску срока и спору.

**[решение]** Дедлайн всегда показывается **двумя строками**: основная — время
операционной зоны (Asia/Tbilisi) с явной меткой зоны; вторая — то же время в
локальной зоне устройства пользователя, если она отличается. Плюс относительный
остаток («через 4 ч 12 мин») как третий, вспомогательный элемент.
Относительное время **никогда не единственное** представление дедлайна.

Ниже дедлайна — **всегда** последствие: что произойдёт при его наступлении, если
никто ничего не сделает. Без последствия таймер не рендерится (проверяется
типом: `Deadline` требует `consequenceKey`) **[решение]**.

**Пауза часов.** При недоступности реестра часы сделки приостанавливаются и
дедлайны сдвигаются (`FUNCTIONAL.md` §3.5). В интерфейсе это отдельное состояние
таймера: отсчёт остановлен, показана причина, показано «новый срок будет назван
после восстановления» — а не молчаливое изменение цифры. Обе стороны видят
одинаково. ▣ C-03.

### 1.3 Брейкпоинты и устройства

| Класс | Ширина | Кто |
|---|---|---|
| `xs` | 360–479 | Покупатель, основной сценарий |
| `sm` | 480–767 | Покупатель |
| `md` | 768–1023 | Планшет, получатель в дороге |
| `lg` | 1024–1439 | Получатель, консоль — основной |
| `xl` | ≥ 1440 | Консоль, застройщик с реестрами |

- **Кабинет покупателя — mobile-first.** Проектируется от 360 px, десктоп — та же
  вертикальная лента, ограниченная по ширине измерения строки (60–75 симв.),
  без превращения в двухколоночную панель. Одна колонка — не ограничение, а
  решение: сделка читается сверху вниз один раз, а не сканируется.
- **Кабинет получателя — desktop-first**, но карточка подтверждения средств и
  экран реквизитов обязаны полностью работать на `xs`: продавец смотрит
  подтверждение с телефона у нотариуса. Это проверяется отдельно **[решение]**.
- **Консоль — desktop-first**, минимум `lg`. Ниже `md` — экран-заглушка «консоль
  не предназначена для телефона», кроме двух исключений, которые обязаны работать
  на телефоне: **экран утверждения выплаты** и **дежурный дашборд** (руководителя
  вызывают ночью). **[решение]**
- Сенсорные цели ≥ 44×44 CSS px на всех классах, включая консоль на планшете.
- Ни один экран не ломается при зуме 200% и при системном шрифте +30%.

### 1.4 Универсальные состояния экрана

Каждый экран специфицирует **все** применимые состояния. Отсутствие спецификации
состояния — незакрытый экран, а не «потом добавим».

| Код | Состояние | Правило |
|---|---|---|
| `SC-load` | Загрузка | Скелет формы контента, не спиннер по центру. Каркас (навигация, блок безопасности, известные из кэша суммы) виден сразу |
| `SC-ready` | Данные есть | — |
| `SC-empty` | Данных нет законно | Объясняет, почему пусто, и что появится. **Пустой ≠ ошибка** |
| `SC-partial` | Часть данных не пришла | Показываем то, что есть; неудавшийся блок — локальная ошибка с повтором. **Экран целиком не падает из-за одного блока** |
| `SC-stale` | Данные из кэша, обновление не удалось | Метка «данные на {время}», кнопка обновления. Денежные суммы в `stale` **помечаются явно** и действия по ним блокируются |
| `SC-error` | Блокирующая ошибка | Что произошло, деньги в безопасности, что делать, повтор, идентификатор обращения |
| `SC-denied` | Доступ запрещён | Что именно недоступно и почему; кто может дать доступ; никогда не «404» вместо «нет прав», кроме случаев, где сам факт существования сделки — тайна |
| `SC-offline` | Нет сети | Баннер, кэш, очередь действий не создаётся: **денежные действия не ставятся в очередь офлайн** |
| `SC-frozen` | Операции приостановлены (комплаенс/инцидент) | Экран читаем, действия недоступны с причиной и сроком |

**Правило загрузки денег:** сумма никогда не отображается «примерно» и не
анимируется от нуля. Пока сумма не подтверждена сервером — на её месте скелет.
Показать неверную сумму хуже, чем не показать никакой.

**Правило ошибки:** ни на одном экране покупателя и получателя сообщение об
ошибке не остаётся без ответа на вопрос «где мои деньги». Шаблон:
`«common.error.<code>.title»` + `«common.error.moneySafe»` + действие + номер
обращения. ▣ C-14.

### 1.5 Постоянный блок безопасности

**[установлено]** Присутствует во всех кабинетах и во всех письмах.

- Кабинет покупателя: закреплён в подвале каждого экрана + развёрнутый экран B-07.
  На экране пополнения — **не в подвале, а непосредственно над реквизитами**.
- Кабинет получателя: подвал + постоянно на экране реквизитов выплаты.
- Консоль: в виде памятки на экранах, где оператор общается со сторонами.
- Не сворачивается, не скрывается, не является модальным окном, которое можно
  закрыть навсегда.
- Содержит **[установлено]**: мы не сообщаем реквизиты по телефону, почте и в
  мессенджере; единственное место, где вы видите реквизиты — этот кабинет; мы
  никогда не просим перевести деньги на другой счёт.
- Содержит действие «сообщить о подозрительном контакте» → §3.7. ▣ C-05.

### 1.6 Уведомления и почта

**[установлено]** Никаких ссылок на действия в письмах. Письмо говорит «есть
обновление по сделке», ссылка ведёт в кабинет, действие совершается там.

Следствие для экранов: при входе по ссылке из письма пользователь попадает на
экран сделки, а не на действие. Если действие требуется — оно уже видно в блоке 2
как единственное. Глубокие ссылки на подтверждение, изменение реквизитов и вывод
средств **не существуют** — эти экраны недостижимы иначе, чем навигацией внутри
сессии **[решение]**.

### 1.7 Доступность — базовая линия для всех экранов

WCAG 2.1 AA как минимум. Проверяется на каждом экране, а не в конце.

- Один `<h1>` на экран; заголовки без пропусков уровней; ориентиры `header/nav/
  main/aside/footer`.
- Статус денег и статус сделки **не передаются одним цветом**: цвет + текст +
  форма индикатора. Дальтоник обязан различать «зарезервировано» и «возвращено».
- Таймер дедлайна: обновление в `aria-live="polite"` не чаще раза в минуту (иначе
  скринридер зачитывает его непрерывно). За 10 минут до конца — одно объявление
  `assertive`.
- Ограничение по времени (фиксация курса) — WCAG 2.2.1: обязана быть возможность
  продлить или пересчитать; истечение не должно приводить к потере введённых
  данных.
- Все действия доступны с клавиатуры; модальные окна с ловушкой фокуса и
  возвратом фокуса; порядок табуляции соответствует визуальному.
- Ошибки формы связаны с полем (`aria-describedby`), сводка ошибок вверху
  фокусируема и ведёт к полю.
- `prefers-reduced-motion` уважается; мерцание таймера и шиммер скелетов
  отключаются.
- Загрузка документов доступна с клавиатуры; drag-and-drop — только дополнение,
  никогда единственный способ.

---

## 2. Модель состояний, на которую опираются экраны

### 2.1 Состояния из продуктовых документов

**[установлено]** (`PRODUCT.md` §7):

```
Сделка:  Черновик → Стороны проверены → Объект проверен → Средства заведены
         → Зарезервированы → Подано на регистрацию
           → Условие подтверждено → Исполнение → Закрыта
           → Условие не наступило → Откат → Средства освобождены

Транш:   ожидает → собирается → собран → удерживается
         → релиз одобрен → выплачивается → выплачен
         → возврат одобрен → возвращается → возвращён

Выплата: создана → отправлена → {исполнена | отклонена | неизвестно}
         неизвестно → (только через сверку) → {исполнена | отклонена}
```

Интерфейс **не изобретает** новых состояний домена. Всё, что ниже — проекция этих
состояний на язык пользователя.

### 2.2 «Где деньги» — проекция для интерфейса

Это единственный источник истины для блока 1 кабинета покупателя, для карточки
подтверждения средств у продавца и для строки денег в консоли. Один код — один
смысл во всех трёх кабинетах, формулировки разные по роли.

| Код | Ключ состояния | Где деньги физически | Что дальше | Что от покупателя | Что видит продавец |
|---|---|---|---|---|---|
| `M-01` | `notFunded` | У покупателя, в его банке | Ждём перевод | Пополнить | Средства не внесены |
| `M-02` | `transferDeclared` | В пути между банками | Зачисление, срок {SLA} | Ничего, ждать | Перевод заявлен, не подтверждён |
| `M-03` | `unidentified` | Пришли на номинальный счёт, **не опознаны** (`suspense`) | Ручной разбор, срок {SLA} | Прислать подтверждение перевода | Средства не внесены (см. §8.4) |
| `M-04` | `heldThirdParty` | На номинальном счёте, **удержаны** | Разбор комплаенсом | Подтвердить родство/происхождение или согласовать возврат | Средства не внесены |
| `M-05` | `onAccountFx` | На счёте покупателя, в валюте перевода | Конвертация по курсу | Подтвердить курс | Средства внесены, не конвертированы |
| `M-06` | `onAccount` | На счёте покупателя, в лари | Резервирование в день подачи | Ничего / забрать в любой момент | Деньги у покупателя, ещё не зарезервированы |
| `M-07` | `partiallyFunded` | Часть на счёте | Добор до ⏱, иначе откат | Дослать {разница} | Собрано {X} из {Y} |
| `M-08` | `overfunded` | На счёте с излишком | Излишек вернём после закрытия/отмены на счёт-источник | Ничего | Сумма собрана |
| `M-09` | `reserved` | На номинальном счёте, **зарезервированы под сделку** | Подача документов, ⏱ | Ничего, ждём подачу | **СРЕДСТВА ПОДТВЕРЖДЕНЫ** |
| `M-10` | `submitted` | Там же, резерв держится | Подтверждение регистрации, ⏱ | Ничего | Документы поданы, резерв держится |
| `M-11` | `releasePending` | Там же | Расчёт исполняется | Ничего | Регистрация подтверждена, расчёт готовится |
| `M-12` | `released` | **У получателя** | Сделка закрывается | Ничего | Расчёт исполнен |
| `M-13` | `payoutUnknown` | Поручение отправлено, подтверждение банка не получено | Сверка следующего банковского дня | Ничего, мы сообщим | Поручение отправлено, ждём подтверждение банка |
| `M-14` | `rollbackInProgress` | На номинальном счёте, резерв снимается | Возврат на счёт покупателя | Ничего | Условие не наступило, резерв снимается |
| `M-15` | `releasedToAccount` | **На счёте покупателя**, свободны | Вывод или новая сделка | Забрать или провести сделку заново | Сделка не состоялась |
| `M-16` | `refundInProgress` | Возврат на счёт-источник в пути | Зачисление в его банке | Ничего | — |
| `M-17` | `refunded` | **У покупателя, в его банке** | Ничего | Ничего | — |
| `M-18` | `frozen` | Где были, движение приостановлено | Разбор, срок | По ситуации | Операции приостановлены |

Модификаторы, накладываемые поверх любого состояния (не заменяют его):

| Модификатор | Смысл | Влияние на интерфейс |
|---|---|---|
| `clockPaused` | Часы сделки остановлены (реестр недоступен) | Таймер замирает, показана причина, все дедлайны помечены «срок будет пересчитан» |
| `quoteExpired` | Окно фиксации курса истекло | Блокирующая карточка пересчёта, суммы в лари скрыты до подтверждения нового курса |
| `requisitesLocked` | Реквизиты выплаты заблокированы резервом | В кабинете получателя форма read-only |
| `requisitesCooling` | Идёт период охлаждения после смены реквизитов | Выплата запрещена, таймер охлаждения виден обеим сторонам |
| `coverageBreach` | Нарушено покрытие клиентских средств | Приём новых сделок остановлен; в кабинетах — баннер о приостановке приёма, **без деталей инцидента** |

**Диаграмма переходов (упрощённая, для интерфейса):**

```
M-01 ──заявил перевод──▶ M-02 ──зачислено, референс распознан──▶ M-05
  │                        │
  │                        ├──референс не распознан──▶ M-03 ──опознано──▶ M-05
  │                        └──отправитель ≠ покупатель──▶ M-04 ──разбор──▶ M-05 | M-16
  │
M-05 ──конвертация──▶ M-06 ──▶ M-07 (недобор) ──добор──▶ M-06
                       │  └──▶ M-08 (переплата)
                       │
                       ├──резерв──▶ M-09 ──подача──▶ M-10
                       │                               ├──условие подтверждено──▶ M-11 ──▶ M-12
                       │                               │                                └──▶ M-13 ──сверка──▶ M-12 | M-14
                       │                               └──дедлайн, условие не наступило──▶ M-14 ──▶ M-15
                       │
                       └──вывод──▶ M-16 ──▶ M-17

M-15 ──вывод──▶ M-16      M-15 ──новая сделка──▶ M-09
Любое ──стоп комплаенса──▶ M-18 ──▶ возврат в исходное или M-16
```

**Тупиков нет:** из каждого состояния есть выход, и для каждого выход по
бездействию — возврат покупателю (красная линия №7). Состояния `M-13` и `M-18`
не имеют автоматического выхода по таймеру — из них выходят только через сверку
или решение человека, и интерфейс это говорит прямо.

### 2.3 Матрица «состояние сделки × главный экран покупателя»

Читается как контракт: для каждого состояния сделки известно, что показывает
каждый из семи блоков. Детали блоков — §3.3.

| Сделка | Блок 1 «где деньги» | Блок 2 «что требуется» | Блок 3 лента | Блок 6 срок и последствие |
|---|---|---|---|---|
| Черновик | `M-01` | Пройти проверку личности | шаг 1 текущий | Нет дедлайна → блок скрыт, вместо него «срок появится после…» |
| Стороны проверены | `M-01` | Ждать проверки объекта | шаг 2 текущий | То же |
| Объект проверен | `M-01` | **Пополнить счёт** | шаг 3 текущий | Дедлайн пополнения → откат сделки |
| Средства заведены | `M-05`/`M-06`/`M-07`/`M-08` | Подтвердить курс / дослать / ничего | шаг 3 пройден | Дедлайн резерва |
| Зарезервированы | `M-09` | Ничего, ждём подачу | шаг 4 текущий | «Если не подадут до ⏱ — резерв снят, деньги остаются у вас» |
| Подано на регистрацию | `M-10` | Ничего, ждём реестр | шаг 5 текущий | «Если регистрация не подтвердится до ⏱ — резерв снят» |
| Условие подтверждено | `M-11` | Ничего | шаг 6 пройден | Срок исполнения расчёта |
| Исполнение | `M-11`/`M-13` | Ничего | шаг 7 текущий | Срок подтверждения банка |
| Закрыта | `M-12` | Ничего. Доступны документы | все пройдены | Блок срока заменён на дату закрытия |
| Условие не наступило | `M-14` | Ничего, идёт откат | шаг 5 не пройден, ветка отката | «Резерв снимается, деньги вернутся на ваш счёт до ⏱» |
| Откат / Средства освобождены | `M-15` | **Забрать деньги или провести сделку заново** | ветка отката завершена | Нет дедлайна. Есть срок хранения средств на счёте |

**Инвариант интерфейса:** транш в нетерминальном состоянии **всегда** имеет
видимый дедлайн. Если сервер вернул нетерминальный транш без дедлайна — это баг
домена (`FUNCTIONAL.md` §2.7); интерфейс показывает `⚠ common.deadline.missing`
и создаёт обращение автоматически, а не рисует пустоту **[решение]**.

### 2.4 Уровни подтверждения средств для получателя

Лестница, по которой продавец понимает, насколько прочна его позиция. Один
компонент ▣ C-08, пять уровней:

| Уровень | Условие | Что это значит для продавца |
|---|---|---|
| `A0` | Сделка заведена | Ничего пока не подтверждено |
| `A1` | Покупатель прошёл проверку | Покупатель существует и проверен |
| `A2` | Средства на счёте покупателя | Деньги есть, но принадлежат покупателю и отзывны |
| `A3` | **Средства зарезервированы под сделку** | Выдано подтверждение резервирования с номером |
| `A4` | Расчёт исполнен | Деньги ушли на его счёт |

Уровень **не** называется гарантией. Разница между `A2` и `A3` объясняется явно —
это ровно то, ради чего продавец приходит в кабинет.

---

# 3. Кабинет покупателя

**Устройство:** mobile-first, 360 px базовая ширина.
**Языки:** русский, английский, грузинский.
**Кто:** иностранец, часто вне страны, часто в другом часовом поясе, сумма для
него значительная, доверия к нам пока нет.

**[открыто]** Существует ли кабинет до заведения сделки оператором (`CABINETS.md`
§8.2). Ниже спроектировано в предположении **«кабинет появляется вместе со
сделкой»**: пользователь получает приглашение, входит и сразу видит сделку. Если
решение будет иным, добавляется экран «сделок пока нет» — он специфицирован в
§3.3/`SC-empty` как заготовка.

## 3.0 Информационная архитектура и каркас

```
┌──────────────────────────────────────┐
│ ▣ C-20 Шапка                         │
│  «buyer.nav.brand»   ⚙  🌐 RU|EN|KA  │   🌐 = ▣ C-21 переключатель языка
├──────────────────────────────────────┤
│                                      │
│           СОДЕРЖИМОЕ ЭКРАНА          │
│                                      │
├──────────────────────────────────────┤
│ ▣ C-05 Блок безопасности (постоянно) │
└──────────────────────────────────────┘
│ ▣ C-22 Нижняя навигация (xs–sm)      │
│  Сделка · Счёт · Документы · Помощь  │
└──────────────────────────────────────┘
```

Четыре пункта навигации, не больше: «Сделка», «Мой счёт», «Документы», «Помощь и
безопасность». Пополнение — **не пункт навигации**, а действие внутри сделки и
счёта: у него нет самостоятельного смысла вне контекста «сколько и зачем».
Проверка личности — не пункт, а перехватывающий мастер: пока она не пройдена,
любой экран, кроме помощи, ведёт на неё.

На `lg+` нижняя навигация превращается в горизонтальную в шапке; содержимое
ограничено по ширине (примерно 65 символов измерения строки) и остаётся одной
колонкой.

**Глобальные баннеры** (над содержимым, стек не более двух одновременно,
приоритет сверху вниз): приостановка операций (`M-18`), нарушение покрытия
(`coverageBreach`, текст без деталей инцидента), истёкшая фиксация курса,
требуется действие по проверке личности, часы приостановлены. ▣ C-13.

---

## 3.1 Экран B-01 — Вход

### Назначение
Впустить того, кого мы уже знаем, и не дать фишингу выглядеть так же.

### Данные
Способ входа (почта / телефон), одноразовый код, признак доверенного устройства,
язык интерфейса.

### Структура (`xs`)

```
┌──────────────────────────────────────┐
│ «buyer.brand.name»        🌐 RU EN KA│
│                                      │
│ H1 «buyer.login.title»               │
│ «buyer.login.subtitle»               │
│                                      │
│ [ Поле: «buyer.login.email.label» ]  │
│   «buyer.login.email.hint»           │
│                                      │
│ [[ «buyer.login.submit» ]]           │
│                                      │
│ ▸ «buyer.login.noAccess»             │
│    «buyer.login.noAccess.body»       │
│                                      │
│ ▣ C-05 Блок безопасности             │
│ «buyer.login.legal» ⚖                │
└──────────────────────────────────────┘
```

Второй шаг — ввод кода: ▣ C-23 OTP-поле, шесть ячеек, автоподстановка из SMS,
вставка целиком, обратный отсчёт до повторной отправки, счётчик оставшихся
попыток.

### Состояния

| Код | Условие | Поведение |
|---|---|---|
| `S-01` | Ввод идентификатора | Базовое |
| `S-02` | Код отправлен | Таймер повторной отправки; ключ `buyer.login.otp.resendIn` с плюрализацией секунд |
| `S-03` | Код неверен | Ошибка под полем, **введённое не стирается**, счётчик попыток виден с первой ошибки, а не с последней |
| `S-04` | Попытки исчерпаны | Временная блокировка с точным временем разблокировки (две зоны, §1.2), путь через поддержку |
| `S-05` | Учётной записи нет | **Не сообщаем, существует ли адрес.** Одинаковый ответ для существующего и несуществующего; «код отправлен, если адрес известен» |
| `S-06` | Вход из новой страны/устройства | Дополнительный фактор + уведомление на все каналы. Экран объясняет почему |
| `SC-error` | Сервис недоступен | «Вход временно недоступен, деньги в безопасности», статус-страница, номер обращения |
| `SC-frozen` | Учётная запись приостановлена | Впускаем, но кабинет в режиме чтения; причина и срок |

### Поведение и крайние случаи
- Ссылка из письма ведёт сюда, а не в действие (§1.6). После входа —
  восстановление намерения: сразу экран сделки.
- Переключатель языка доступен **до** входа и запоминается в устройстве до
  профиля.
- Вставка кода из буфера обмена не должна ломаться о шесть отдельных ячеек.
- Автозаполнение менеджеров паролей не блокируется.
- Экран не показывает ни сумм, ни имён сторон, ни названия объекта — вход виден
  тому, кто взял телефон.

### Адаптивность
Одна колонка на всех классах, максимум 400 px ширины формы, вертикально
по центру на `lg+`. Клавиатура на `xs` не перекрывает кнопку: она в потоке, не
`position: fixed`.

### Доступность
`autocomplete="one-time-code"`, `inputmode="numeric"`; ошибка связана с группой
ячеек, а не с отдельной ячейкой; таймер повторной отправки в `aria-live=polite`.

### Компоненты
C-05, C-14, C-20, C-21, C-23, C-24 (поле), C-25 (кнопка).

---

## 3.2 Экран B-02 — Проверка личности

### Назначение
Собрать то, без чего сделка не начнётся, не потеряв клиента на середине. Это
самый длинный и самый бросаемый путь в продукте.

### Форма — пошаговый мастер

| Шаг | Содержание | Может быть отложен |
|---|---|---|
| 1 | Документ, по возможности NFC-чтение чипа | нет |
| 2 | Селфи с проверкой живости **или альтернатива** | нет, но есть второй путь |
| 3 | Адрес проживания | нет |
| 4 | Налоговое резидентство | нет |
| 5 | **Источник средств** | нет — но сохраняется по частям |
| 6 | Декларация о публичной должности ⚖ | нет |
| 7 | Обзор и отправка | — |

### Каркас мастера

```
┌──────────────────────────────────────┐
│ ← «buyer.kyc.back»      «шаг 3 из 7» │  ▣ C-26 индикатор шагов
│ ▓▓▓▓▓░░░░░░░  с подписями шагов      │
├──────────────────────────────────────┤
│ H1 «buyer.kyc.step3.title»           │
│ «buyer.kyc.step3.why»   ← зачем      │
│                                      │
│      ... поля шага ...               │
│                                      │
│ [[ «buyer.kyc.continue» ]]           │
│ [ «buyer.kyc.saveExit» ]             │
├──────────────────────────────────────┤
│ ▣ C-05                               │
└──────────────────────────────────────┘
```

**«Зачем» на каждом шаге** — не украшение. Иностранец отдаёт паспорт и выписку
по счёту незнакомому сервису; если не объяснить, зачем именно это нужно, он
уходит. Ключ `buyer.kyc.stepN.why`, копи-бриф: одна фраза, причина в терминах
закона и его же безопасности, без канцелярита, без «в соответствии с
требованиями законодательства» без указания какого.

### Сквозные состояния мастера

| Код | Условие | Поведение |
|---|---|---|
| `S-01` | Не начат | Экран-вход в проверку: сколько шагов, сколько времени займёт **[гипотеза: 12–20 минут, уточнить на пилоте]**, что подготовить заранее (список документов до старта, а не по ходу) |
| `S-02` | В процессе | Автосохранение каждого шага; выход и возврат без потери |
| `S-03` | Отправлено, на проверке | Экран ожидания: срок решения (норматив ≤4 рабочих часов, `PRODUCT.md` §13), что происходит, можно закрыть страницу |
| `S-04` | Нужны уточнения | **Не «отказ».** Список того, что заменить, с причиной по каждому пункту; остальное принято и не переделывается |
| `S-05` | Отклонено | Причина в допустимых пределах раскрытия ⚖, путь обжалования, что происходит с уже внесёнными деньгами (`M-16`) |
| `S-06` | Пройдено | Свёрнутая карточка в профиле, дата и срок следующей перепроверки |
| `SC-error` | Провайдер идентификации недоступен | Не теряем введённое; предлагаем альтернативу (видеозвонок) сразу, а не после трёх попыток |

### Шаг 1 — документ

- Выбор страны и типа документа → **список требований меняется**.
- NFC-чтение: анимированная инструкция «приложите документ к телефону», состояния
  `поиск / чтение / успех / чип не читается`. При неудаче — тихий переход к
  фотографированию, без обвинения пользователя.
- Съёмка: рамка, автоспуск при фокусе, немедленная проверка качества (блики,
  обрез, размытость) с конкретной причиной переснять — не «плохое фото».
- Крайние случаи: документ без MRZ; два гражданства (какой документ выбрать —
  подсказка); срок действия истекает в ближайшие 3 месяца → предупреждение до
  загрузки, а не после проверки; имя в документе латиницей ≠ имени в профиле →
  §5.4, поле `nameAsInDocument` фиксируется отдельно.

### Шаг 2 — живость и **альтернатива биометрии**

**[установлено]** Отказ от селфи — не основание для отказа в обслуживании.

```
┌──────────────────────────────────────┐
│ H1 «buyer.kyc.liveness.title»        │
│ «buyer.kyc.liveness.why»             │
│                                      │
│ [[ «buyer.kyc.liveness.start» ]]     │
│                                      │
│ ─────────────────────────────────    │
│ «buyer.kyc.liveness.alt.title»       │
│ «buyer.kyc.liveness.alt.body»        │
│ [ «buyer.kyc.liveness.alt.choose» ]  │
└──────────────────────────────────────┘
```

Альтернатива **видна сразу**, а не после провала. Копи-бриф
`buyer.kyc.liveness.alt.body`: сказать прямо, что отказ от селфи не лишает
обслуживания, и что проверка будет пройдена с оператором. Запрещено: «в порядке
исключения», «мы можем пойти навстречу» — это ставит право в позицию одолжения.

**Ветка видеозвонка** — отдельный подэкран `B-02/alt`:

| Состояние | Что видит клиент |
|---|---|
| Выбор слота | Календарь с явным часовым поясом **его** устройства и Тбилиси, языки оператора (RU/EN/KA) как фильтр, длительность |
| Слот занят при подтверждении | Немедленный пересчёт списка, введённое не теряется |
| Назначено | Карточка с датой в двух зонах, что подготовить (оригинал документа), как войти, кнопки «перенести»/«отменить» |
| Напоминание | За 24 ч и за 1 ч, во все каналы |
| Пропущен | Не наказываем: «выберите новый слот», счётчик пропусков виден нам, не ему |
| Идёт | Ссылка активна за 5 минут до, проверка камеры/микрофона до входа |
| Проведён | Шаг закрыт, мастер продолжается автоматически |
| Нет свободных слотов | Честно: «ближайший через N дней» + альтернатива визита в офис партнёра **[открыто: есть ли офлайн-точка на пилоте]** |

Доступность: видеозвонок — не единственная альтернатива для человека, который не
может говорить или видеть. Третий путь (визит, доверенное лицо с доверенностью)
обозначен ключом `buyer.kyc.liveness.alt.accessibility` **[открыто §8.14]**.

### Шаги 3–4 — адрес и налоговое резидентство

Обычная форма, правила §1.7 и `form-design`: одна колонка, метки сверху,
валидация на потере фокуса, помеченное необязательное (не обязательное).
Крайние случаи: адрес без индекса; страна без деления на регионы; несколько
налоговых резидентств (добавляемые строки, у каждой — TIN или причина его
отсутствия ⚖); отсутствие TIN в юрисдикции — выбор причины из перечня, не
свободный текст.

### Шаг 5 — **источник средств как разговор**

Самый тяжёлый шаг. Проектируется как последовательность вопросов, а не как
загрузчик файлов. Ошибка «клиент грузит не то → цикл повторяется» стоит нам
4–8 человеко-часов и клиенту недели.

**5.1 — Вопрос, а не форма.**

```
┌──────────────────────────────────────┐
│ H1 «buyer.sof.q1.title»              │
│    (одна фраза: откуда деньги)       │
│ «buyer.sof.q1.help»                  │
│                                      │
│ ▣ C-27 Список выбора, множественный  │
│  ┌────────────────────────────────┐  │
│  │ ☐ «buyer.sof.type.salary»      │  │
│  │   «...salary.example»          │  │
│  ├────────────────────────────────┤  │
│  │ ☐ «buyer.sof.type.business»    │  │
│  │ ☐ «buyer.sof.type.dividends»   │  │
│  │ ☐ «buyer.sof.type.propertySale»│  │
│  │ ☐ «buyer.sof.type.businessSale»│  │
│  │ ☐ «buyer.sof.type.investments» │  │
│  │ ☐ «buyer.sof.type.inheritance» │  │
│  │ ☐ «buyer.sof.type.gift»        │  │
│  │ ☐ «buyer.sof.type.savings»     │  │
│  │ ☐ «buyer.sof.type.loan»        │  │
│  │ ☐ «buyer.sof.type.crypto»  ⚠   │  │
│  │ ☐ «buyer.sof.type.other»       │  │
│  └────────────────────────────────┘  │
│ [[ «buyer.sof.q1.next» ]]            │
└──────────────────────────────────────┘
```

У каждого варианта — подпись-пример одной строкой (`...example`), чтобы человек
узнал свой случай, а не угадывал категорию. Выбор `crypto` **[установлено]**
криптоактивы вне периметра (`PRODUCT.md` §5) → немедленное честное объяснение и
предложение указать иной источник или прекратить; не «загрузите документы», а
затем отказ через неделю.

**5.2 — Сколько из какого источника.**

```
┌──────────────────────────────────────┐
│ H1 «buyer.sof.q2.title»              │
│ «buyer.sof.q2.help»                  │
│                                      │
│ Нужно объяснить: {данные: deal.amount}│
│ ▣ C-09 Покрытие                      │
│ ▓▓▓▓▓▓▓▓░░░░  «buyer.sof.coverage»   │
│  «buyer.sof.coverage.target»  ≥90%   │
│                                      │
│ Продажа недвижимости [ 120 000 ▾GEL ]│
│ Трудовой доход       [  60 000 ▾GEL ]│
│ Накопления           [  20 000 ▾GEL ]│
│ ····                                 │
│ «buyer.sof.q2.remaining»  {сумма}    │
│ [[ «buyer.sof.q2.next» ]]            │
└──────────────────────────────────────┘
```

Показ покрытия клиенту — **[решение]**: правило «подтверждённое происхождение
закрывает не менее 90% суммы» (`CABINETS.md` §5.2) объясняет клиенту, почему
недостаточно одной справки, и превращает шаг из бюрократии в понятную задачу.
Риск: подсказывает, как подогнать. Смягчение: показываем **порог покрытия**, но
не показываем, какие именно детекторы сработали. Вынесено в §8.5.

**5.3 — Что приложить именно для этого типа.**

Для каждого выбранного типа — свой экран со своим списком. Не общий загрузчик.

```
┌──────────────────────────────────────┐
│ «источник 2 из 3»                    │
│ H1 «buyer.sof.doc.propertySale.title»│
│ «buyer.sof.doc.propertySale.intro»   │
│                                      │
│ ▣ C-28 Требование к документу        │
│ ┌──────────────────────────────────┐ │
│ │ 1 «...contract.title»            │ │
│ │   «...contract.whatMustBeVisible»│ │
│ │   ▸ «common.doc.showExample»     │ │
│ │   [ Загрузить ]  ● не приложено  │ │
│ ├──────────────────────────────────┤ │
│ │ 2 «...bankCredit.title»          │ │
│ │   «...bankCredit.whatMustBeVisible»│
│ │   ✓ принято · {файл} · {дата}    │ │
│ ├──────────────────────────────────┤ │
│ │ 3 «...proofOfOwnership.title»    │ │
│ │   ⟳ на проверке                  │ │
│ ├──────────────────────────────────┤ │
│ │ 4 «...optional.title» «common.optional»│
│ └──────────────────────────────────┘ │
│ [[ «buyer.sof.doc.next» ]]           │
└──────────────────────────────────────┘
```

Ключевые элементы каждого требования (▣ C-28):

- **что это за документ** — название на языке страны клиента, не наш термин;
- **что на нём должно быть видно** — «сумма, дата, ваше имя, название банка,
  печать или электронная подпись». Это то, что снимает половину повторов;
- **пример** — раскрывающийся образец с закрашенными данными; не модальное окно
  поверх формы, а раскрытие в потоке, чтобы можно было смотреть и загружать;
- **статус**: не приложено / загружается / на проверке / **принято** / **нужно
  заменить** (с конкретной причиной и указанием, что именно не видно);
- **необязательные** помечены явно, обязательные — нет (правило `form-design`).

Списки требований по типам источника — **данные, а не вёрстка**: конфигурация
с версией, ключи вида `buyer.sof.doc.<type>.<docId>.*`. Комплаенс меняет списки
без релиза фронтенда **[решение]**.

**5.4 — Связка «ваше имя ↔ движение денег».**

`FUNCTIONAL.md` требует связать имя покупателя с движением денег. Отдельный
короткий шаг: «покажите выписку, где видно поступление этой суммы на ваш счёт» с
объяснением, зачем — иначе клиент воспринимает это как повторный запрос того же.

**Состояния шага 5**

| Код | Условие | Поведение |
|---|---|---|
| `S-01` | Ничего не выбрано | Кнопка «дальше» недоступна с причиной рядом, не молча серая |
| `S-02` | Покрытие < порога | Предупреждение до отправки: «объяснено {X}% — этого не хватит», с предложением добавить источник. Отправить всё равно можно **[решение]**: запрещать нельзя, иначе клиент застревает без выхода |
| `S-03` | Документ отклонён | Красный статус у **конкретного** требования, причина, остальные не сбрасываются |
| `S-04` | Файл не того формата/размера | Ошибка до загрузки, лимиты названы заранее |
| `S-05` | Документ на языке, который мы не читаем | Просим перевод; список принимаемых языков назван заранее, а не после отказа **[открыто §8.6]** |
| `S-06` | Загрузка прервалась | Возобновление, файл не теряется, прогресс по каждому файлу отдельно |
| `S-07` | Всё принято | Шаг сворачивается в строку с галочкой; вернуться и посмотреть можно, менять — через обращение |

### Шаг 6 — декларация о публичной должности ⚖

Формулировки — через `legal-compliance-ru`. Интерфейсно: вопрос с вариантами
«нет / я / мой родственник или близкий», раскрытие полей только при «да»,
объяснение, что «да» не означает отказ, а означает дополнительные вопросы. Это
важно: без этой фразы люди отвечают неправдиво.

### Шаг 7 — обзор

Все ответы одним списком с возможностью вернуться к каждому. Согласия ⚖
отдельными чекбоксами (не одним общим), каждое со ссылкой на полный текст,
версия согласия фиксируется. Кнопка отправки — единственная.

### Адаптивность
Мастер одноколоночный на всех классах. На `lg+` — индикатор шагов слева
вертикально, содержимое справа, ширина содержимого не растёт. Камера и NFC —
только мобильный путь; на десктопе предлагается продолжить с телефона по
защищённой ссылке (одноразовая, короткоживущая, без данных в URL).

### Доступность
Каждый шаг — свой `<h1>` и заголовок документа; при переходе фокус ставится на
заголовок шага; прогресс объявляется как «шаг N из 7, {название}»; загрузка
файлов доступна кнопкой; статусы документов — текст + иконка, не только цвет.

### Компоненты
C-05, C-09, C-13, C-14, C-24…C-29, C-32 (загрузка файла), C-33 (образец
документа).

---

## 3.3 Экран B-03 — Сделка (главный)

### Назначение
Единственный экран, ради которого клиент заходит. Отвечает на три вопроса без
скролла в первом экране и без вкладок целиком.

**Решение о структуре:** один вертикальный экран, семь блоков, читается сверху
вниз **[установлено]**. Вкладок нет: вкладка прячет состояние, а прятать
состояние денег запрещено.

### Данные экрана
Состояние сделки и транша; `M-*` состояние денег; суммы (сделки, на счёте,
комиссии, к получению продавцом) в минорных единицах; курс и окно фиксации;
дедлайн с последствием; данные объекта из реестра и из договора; стороны и их
уровни проверки; список документов; лента событий.

### Каркас (`xs`)

```
┌──────────────────────────────────────┐
│ ▣ C-13 глобальные баннеры (если есть)│
├══════════════════════════════════════┤
│ БЛОК 1 · ГДЕ МОИ ДЕНЬГИ   ▣ C-01     │
│  ● «buyer.deal.whereMoney.<M>.title» │
│    «buyer.deal.whereMoney.<M>.body»  │
│    {сумма}                           │
├══════════════════════════════════════┤
│ БЛОК 2 · ЧТО ТРЕБУЕТСЯ ОТ ВАС ▣ C-02 │
│  «...action.<state>.title»           │
│  [[ «...action.<state>.cta» ]]       │
├──────────────────────────────────────┤
│ БЛОК 6 · СРОК  ▣ C-03  (на xs — сюда)│
│  ⏱ {дедлайн, 2 зоны}                 │
│  «common.deadline.consequence.<x>»   │
├──────────────────────────────────────┤
│ БЛОК 5 · ДЕНЬГИ   ▣ C-04             │
├──────────────────────────────────────┤
│ БЛОК 3 · ЛЕНТА СДЕЛКИ  ▣ C-06        │
├──────────────────────────────────────┤
│ БЛОК 4 · ОБЪЕКТ   ▣ C-07             │
├──────────────────────────────────────┤
│ БЛОК 7 · СТОРОНЫ И ДОКУМЕНТЫ ▣ C-10  │
├──────────────────────────────────────┤
│ ▣ C-05 Блок безопасности             │
└──────────────────────────────────────┘
```

**Порядок блоков отличается от нумерации намеренно.** Нумерация — из
`CABINETS.md`; порядок на экране — по срочности для читателя: где деньги → что
делать → до какого срока → сколько → как идёт → что за объект → кто участвует.
Блок 6 поднят под блок 2, потому что действие без срока бессмысленно.

### Блок 1 — «Где мои деньги» ▣ C-01

Самый верх, всегда виден, **не сворачивается, не скроллится за пределы первого
экрана**. На `xs` занимает не более 40% высоты первого экрана, чтобы блок 2 был
виден без скролла — проверяется на 360×640.

Анатомия: индикатор состояния (цвет + форма + текст) · заголовок · тело в одну
или две фразы · сумма · при необходимости — одна вспомогательная ссылка.

**Все состояния блока 1.** Копи-брифы обязательны: тексты не финализированы.

| `M` | Заголовок (о чём) | Тело (о чём) | Сумма | Дополнительно |
|---|---|---|---|---|
| `M-01` `notFunded` | Деньги ещё не внесены | Что нужно перевести и что до перевода ничего не происходит | Требуемая сумма | Ссылка на реквизиты |
| `M-02` `transferDeclared` | Перевод отправлен, ждём зачисления | Обычный срок зачисления; что мы сообщим сами | Ожидаемая | «Что делать, если задержалось» |
| `M-03` `unidentified` | Поступление получено, но не опознано | Перевод пришёл без распознанного референса, деньги на сделку **не зачислены**, идёт ручной разбор, срок | Не показываем как «ваш баланс» | Действие: прислать подтверждение перевода |
| `M-04` `heldThirdParty` | Перевод удержан: отправитель не вы | Прямо: платёж от другого лица остановлен, включая супруга и вашу компанию; что можно сделать | Удержанная сумма, помечена как недоступная | Действие: подтвердить или согласовать возврат |
| `M-05` `onAccountFx` | На вашем счёте, в {валюта} | Ждём вашего подтверждения курса перед конвертацией в лари | В валюте | Действие: подтвердить курс |
| `M-06` `onAccount` | На вашем счёте. Забрать можно в любой момент | Что резерв будет установлен в день подачи документов, и до этого деньги ваши | В лари | Ссылка «вывести» |
| `M-07` `partiallyFunded` | На счёте меньше, чем нужно | Разница и срок добора; что произойдёт, если не доберём | {есть} из {нужно} | Действие: дослать |
| `M-08` `overfunded` | На счёте больше, чем нужно | Излишек вернём **после закрытия или отмены**, только на счёт-источник — не сейчас | Излишек отдельной строкой | — |
| `M-09` `reserved` | Зарезервированы под сделку до ⏱ | Деньги на счёте номинального держания в {банк}, не у продавца и не на нашем балансе. **Вносящая сторона вправе отозвать их до наступления условия** | Сумма резерва | Ссылка на подтверждение резервирования |
| `M-10` `submitted` | Зарезервированы, документы поданы | Ждём подтверждения регистрации из реестра; резерв держится до ⏱ | Сумма резерва | Номер заявления |
| `M-11` `releasePending` | Регистрация подтверждена, расчёт исполняется | Что расчёт исполняется сегодня; никаких действий не требуется | Сумма | — |
| `M-12` `released` | Расчёт исполнен | Деньги у продавца; ссылка на документы по сделке | Итог | Документы |
| `M-13` `payoutUnknown` | Поручение отправлено, подтверждение банка ещё не получено | Честно: банк не подтвердил, повторно **не отправляем**, проверим по выписке следующего банковского дня, сообщим сами | Сумма | Ожидаемое время ответа |
| `M-14` `rollbackInProgress` | Условие не наступило, резерв снимается | Что деньги вернутся **на ваш счёт в сервисе** (не в банк) до {срок}, и сделку можно провести заново | Сумма | Что удержано (спред) ⚖ |
| `M-15` `releasedToAccount` | Возвращены на ваш счёт — регистрация не состоялась | Деньги снова ваши и отзывны; два пути: вывести на счёт-источник или провести сделку заново | Доступный остаток | Два равнозначных действия |
| `M-16` `refundInProgress` | Возврат на ваш счёт в банке отправлен | Только на счёт-источник, на ваше имя; обычный срок зачисления | Сумма | Реквизиты получателя (маскированные) |
| `M-17` `refunded` | Деньги вернулись в ваш банк | Дата и сумма; что удержано и почему ⚖ | Сумма | Документ о возврате |
| `M-18` `frozen` | Операции по сделке приостановлены | Что деньги на месте и никуда не двигаются; что происходит разбор; срок; к кому обратиться | Сумма, помечена как недоступная | Номер обращения |

**Отдельно про откат.** Три состояния отката (`M-14 → M-15 → опционально M-16/
M-17`) — самое опасное место продукта с точки зрения доверия: клиент узнаёт, что
сделка не состоялась. Требования к блоку в этих состояниях:

- Слово «откат» в интерфейсе покупателя не используется — это наш внутренний
  термин. Используется «сделка не состоялась» / «резерв снят».
- Причина названа **фактом**, а не оценкой: «регистрация не подтвердилась до
  {дата, время}», не «продавец не успел».
- **Никакой вины.** Ни продавцу, ни покупателю.
- Сразу видно, **что удержано** и почему (спред на конвертации удерживается,
  если конвертация состоялась — `FUNCTIONAL.md` §4.4). Эта строка ⚖ и обязана
  совпадать с офертой дословно; она также обязана быть показана **до** сделки,
  а не в момент отката.
- Два действия равнозначны: вывести или провести заново. Мы не подталкиваем к
  повтору — подталкивание в момент разочарования читается как манипуляция.
- Ничего не исчезает: лента, объект, документы остаются доступны.

**Состояния загрузки и ошибки блока 1**

| Состояние | Поведение |
|---|---|
| `SC-load` | Скелет в форме карточки; **сумма скелетом, не нулём** |
| `SC-stale` | Данные из кэша: карточка помечена «на {время}», все действия недоступны с причиной |
| `SC-error` | Карточка не отрисовалась: «мы не смогли показать состояние средств; деньги в безопасности» + повтор + номер обращения. Экран остальными блоками работает |

### Блок 2 — «Что требуется от вас сейчас» ▣ C-02

**Ровно одно действие.** Не список, не «рекомендуем также». Если действий два —
показывается более срочное, второе уходит в блок, к которому относится.
Исключение единственное: `M-15`, где два исхода равноправны по смыслу и выбор за
клиентом.

Если действий нет — «Ничего, ждём {кого} — {чего именно}» с указанием ожидаемого
срока. Ключ `buyer.deal.action.waiting.<subject>`; **пустая формулировка «ждите»
без субъекта и срока запрещена**.

| Состояние | Действие | Ключ CTA |
|---|---|---|
| Проверка не пройдена | Продолжить проверку личности | `buyer.deal.action.kyc.cta` |
| Объект не проверен | — (ждём нас) | `...waiting.platform` |
| `M-01` | Пополнить счёт | `...topup.cta` |
| `M-02` | — (ждём банк) | `...waiting.bank` |
| `M-03` | Прислать подтверждение перевода | `...proveTransfer.cta` |
| `M-04` | Разобраться с платежом от третьего лица | `...thirdParty.cta` |
| `M-05` | Подтвердить курс и сумму | `...confirmRate.cta` |
| `M-07` | Дослать {разница} | `...topupRest.cta` |
| `M-06`, `M-09`, `M-10`, `M-11` | — (ждём продавца / реестр / расчёт) | `...waiting.*` |
| `M-15` | Вывести · Провести сделку заново | `...withdraw.cta` · `...restart.cta` |
| `quoteExpired` | Подтвердить новую сумму | `...requote.cta` |
| `M-18` | Связаться с поддержкой | `...support.cta` |

Состояния кнопки: обычное / в работе (спиннер внутри, ширина не прыгает —
резервируется место под самый длинный из трёх языков) / недоступно с видимой
причиной / успех (кнопка заменяется результатом, не исчезает молча).

### Блок 3 — «Лента сделки» ▣ C-06

**[установлено]** Один компонент, три представления по роли. Покупатель видит
человеческие формулировки, не технические статусы.

```
✓ «buyer.timeline.step.kycDone»          {дата}
✓ «buyer.timeline.step.propertyChecked»  {дата}
✓ «buyer.timeline.step.fundsOnAccount»   {дата}
◉ «buyer.timeline.step.reserved»         ⏱ {до}
○ «buyer.timeline.step.submitted»
○ «buyer.timeline.step.registered»
○ «buyer.timeline.step.settled»
```

- Три вида шага: пройден (с датой), текущий (со сроком), предстоящий.
- Ветка отката показывается **как ветка, а не как ошибка**: при `M-14` шаги
  «регистрация» и «расчёт» помечаются непройденными, добавляются шаги «резерв
  снят» и «деньги на вашем счёте».
- Раскрытие шага даёт подробность: что произошло, когда, кто (роль, не имя
  сотрудника), ссылка на документ, если есть.
- Пауза часов отображается вставкой в ленту: «часы остановлены с {время},
  причина, срок будет пересчитан».
- Пустое состояние: минимум один шаг всегда есть (сделка заведена).

### Блок 4 — «Объект» ▣ C-07

Адрес, кадастровый код, площадь, текущий собственник **по данным реестра**, дата
выписки, актуальность («данные на {дата}»).

**Расхождение с договором показывается красным и блокирует переход дальше**
**[установлено]**. Интерфейсно:

```
⚠ «buyer.property.mismatch.title»
  ┌───────────┬──────────────┬──────────────┐
  │ поле      │ в договоре   │ в реестре    │
  ├───────────┼──────────────┼──────────────┤
  │ площадь   │ {68.4 м²}    │ {66.1 м²}    │
  │ собственник│ {…}         │ {…}          │
  └───────────┴──────────────┴──────────────┘
  «buyer.property.mismatch.whatNow»
```

Блокировка означает: блок 2 показывает не «пополнить», а «расхождение
разбирается»; действие пополнения недоступно **с причиной**. Крайние случаи:
данные реестра недоступны (показываем последнюю выписку с датой и явной пометкой
«не актуализировано»); обременение появилось после проверки — это отдельное
предупреждение, не «расхождение».

### Блок 5 — «Деньги» ▣ C-04

Единый формат везде, чтобы цифры не расходились между экранами **[установлено]**.

```
«buyer.money.dealAmount»        {217 000 ₾}
«buyer.money.onAccount»         {217 000 ₾}
──────────────────────────────────────────
«buyer.money.rate»    {1 USD = 2,71 ₾}
      ⏱ «buyer.money.rate.lockedUntil» {время, 2 зоны}
«buyer.money.fee»               {2 605 ₾}  ▸ из чего
«buyer.money.payeeReceives»     {214 395 ₾}
```

**[открыто §8.1]** Показывать ли комиссию отдельной строкой. Спроектировано с
раскрытием — это позиция `CABINETS.md` §8.1 и требование потребительского права;
раскрытие идёт со ссылкой «из чего состоит» (комиссия за организацию расчёта,
спред на конвертации — разные вещи и разный НДС, `FUNCTIONAL.md` §4.1).

Правила: суммы никогда не обрезаются; валюта всегда рядом с числом; курс всегда
с направлением («1 USD = X ₾», а не «2,71»); в состоянии `quoteExpired` суммы в
лари **скрываются**, а не показываются устаревшими.

### Блок 6 — «Срок» ▣ C-03

```
⏱ {31 августа, 18:00 (Тбилиси)}
   {17:00 по вашему времени}
   «common.deadline.remaining» {через 4 ч 12 мин}
   ─────────────────────────────────────────
   «common.deadline.consequence.rollback»
```

Последствия по типам дедлайна (ключи `common.deadline.consequence.*`):
`topup` — сделка не начнётся, деньги останутся у вас;
`reserve` — резерв будет снят автоматически, деньги останутся у вас;
`registration` — резерв снят, деньги останутся у вас, сделку можно провести
заново; `quote` — курс будет пересчитан, вы увидите новую сумму до подтверждения;
`cooldown` — изменение реквизитов вступит в силу.

Состояния: обычный · менее суток · менее часа (усиление + объявление
скринридеру) · **истёк, обработка идёт** (не «просрочен», а «срок наступил,
выполняется то-то») · **приостановлен** (часы стоят) · отсутствует (баг, см.
§2.3).

### Блок 7 — «Стороны и документы» ▣ C-10

Стороны: роль, отображаемое имя, **уровень проверки** (не персональные данные
второй стороны — покупатель не видит паспорт продавца). Документы: список с
типом, датой, действием «посмотреть». Просмотр — с водяным знаком, содержащим
идентификатор смотрящего и время, каждый просмотр логируется **[установлено]**;
перед первым просмотром — однократное объяснение, что просмотр фиксируется.

### Состояния экрана целиком

| Код | Поведение |
|---|---|
| `SC-load` | Скелет: каркас + блок безопасности сразу; блоки 1, 2, 6 — приоритет загрузки; блоки 3, 4, 7 — вторая волна |
| `SC-empty` | Сделок нет (если решение §8.2 будет «кабинет до сделки»): объяснение, что сделку заводит оператор, что можно сделать пока (пройти проверку), контакт |
| `SC-partial` | Не пришёл блок объекта или лента — локальная ошибка внутри блока с повтором; блоки 1, 2, 6 обязаны отрисоваться, иначе экран уходит в `SC-error` |
| `SC-error` | Полноэкранная ошибка с ответом на вопрос «где деньги» из кэша, если он есть, и явной пометкой, что это кэш |
| `SC-denied` | Нет доступа к сделке (сменился участник): что произошло, к кому обратиться |
| `SC-frozen` | Все действия недоступны с причиной; экран читаем полностью |
| Несколько сделок | Над блоком 1 — переключатель сделок (▣ C-30); при одной сделке переключатель не рендерится |

### Крайние случаи
- Сделка отменена оператором до пополнения — состояние показывается, история
  доступна, деньги `M-01`.
- Сделка с несколькими траншами (рассрочка, P2): блок 5 показывает график, блок 1
  — состояние **ближайшего активного транша**, с явной пометкой «транш 3 из 48».
- Покупатель участвует в двух сделках с одним объектом — переключатель обязателен,
  адрес в заголовке.
- Изменение реквизитов продавца во время сделки → баннер: расчёт приостановлен на
  период охлаждения, срок, что это защитная мера.
- Смена состояния во время просмотра: обновление без потери позиции скролла;
  если изменилось состояние блока 1 — короткое ненавязчивое выделение, без
  модального окна. Модальное окно поверх денег — только при `quoteExpired` и
  `M-18`.

### Адаптивность

| Класс | Поведение |
|---|---|
| `xs`–`sm` | Одна колонка, порядок как выше. Блоки 1+2 — первый экран без скролла на 360×640 |
| `md` | То же, ширина ограничена, боковые поля растут |
| `lg`+ | Одна колонка по центру; блоки 1, 2, 6 могут быть закреплены в липкой шапке при скролле (компактный вид ▣ C-01 `compact`) |

Липкая компактная версия блока 1 на `lg+` — **[решение]**: длинная лента уводит
состояние денег за экран, а это единственное, что не должно исчезать.

### Компоненты
C-01…C-07, C-09, C-10, C-13, C-14, C-30.

---

## 3.4 Экран B-04 — Пополнение

### Назначение
Провести трансграничный перевод так, чтобы деньги дошли с первого раза, с
распознанным референсом, от правильного плательщика, в понятной сумме.
Это экран, на котором продукт зарабатывает и на котором чаще всего ломается.

### Данные
Требуемая сумма в лари; котировка (курс, спред, окно фиксации, время истечения);
реквизиты номинального счёта (получатель, банк, IBAN, SWIFT, назначение,
**референс**); порог для карт; данные профиля плательщика; статус входящих.

### Структура (`xs`)

```
┌──────────────────────────────────────┐
│ ← «buyer.topup.back»                 │
│ H1 «buyer.topup.title»               │
│                                      │
│ ① «buyer.topup.step.amount»          │
│ ▣ C-11 Калькулятор                   │
│ ┌──────────────────────────────────┐ │
│ │ «...youSend»    [ 80 000 ][USD ▾]│ │
│ │        ⇅                          │ │
│ │ «...weCredit»   [ 213 495 ][GEL] │ │
│ │ ────────────────────────────────  │ │
│ │ «...rate» {1 USD = 2,71 ₾}        │ │
│ │ ▸ «...breakdown»                  │ │
│ │    «...marketRate» · «...spread»  │ │
│ │    · «...bankFees.warning»        │ │
│ │ ⏱ ▣ C-12 «...lockedFor» {09:41}   │ │
│ │ «...needed» {217 000 ₾}  ⚠ разница│ │
│ └──────────────────────────────────┘ │
│                                      │
│ ② «buyer.topup.step.method»          │
│ ▣ C-31 Выбор способа                 │
│  ◉ «...method.transfer»  «...recommended»│
│  ○ «...method.payByBank» «...availability»│
│  ○ «...method.card»      «...limit»  │
│                                      │
│ ③ «buyer.topup.step.payerRule»       │
│ ⚠ ▣ C-05b Правило плательщика        │
│   «buyer.topup.payerRule.body»       │
│   ☐ «buyer.topup.payerRule.confirm»  │
│                                      │
│ ④ «buyer.topup.step.requisites»      │
│ ▣ C-05 Блок безопасности (здесь!)    │
│ ▣ C-16 Реквизиты                     │
│  «...beneficiary»  {…}          [⧉]  │
│  «...bank»         {…}          [⧉]  │
│  «...iban»         {…}          [⧉]  │
│  «...swift»        {…}          [⧉]  │
│  ┌──────────────────────────────────┐│
│  │ «...reference»  {SD-7K42-19}  [⧉]││
│  │ ⚠ «...reference.warning»          ││
│  └──────────────────────────────────┘│
│  [ «...copyAll» ]  [ «...sendToEmail» ]│
│                                      │
│ ⑤ [[ «buyer.topup.declareSent» ]]    │
│                                      │
│ ⑥ ▣ C-15 Статус зачисления           │
└──────────────────────────────────────┘
```

### Калькулятор валюты ▣ C-11

- **Двунаправленный**: «я отправлю X» ↔ «должно получиться Y». Оба поля
  редактируемые, второе пересчитывается. Клиент думает и так и так.
- Ввод денег: числовая клавиатура, разделители групп по локали, вставка из буфера
  с очисткой форматирования, ноль не показывается плейсхолдером.
- Под калькулятором — **сколько нужно по сделке** и разница, если введённое
  меньше или больше. Разница — не ошибка, а информация: «этого хватит на
  {N}%», «останется излишек {сумма}».
- **Раскрытие состава** (`...breakdown`): рыночный курс, наш спред, комиссия за
  услугу. Раскрыто по требованию, но раскрытие есть всегда **[установлено]**.
- **Предупреждение о чужих комиссиях** (`...bankFees.warning`): банк-отправитель
  и корреспондент могут удержать своё, и до нас дойдёт меньше. Копи-бриф: назвать
  это прямо и объяснить, что делать (перевод с опцией «все расходы за счёт
  отправителя», где доступна). Без этого предупреждения мы получаем недоплату и
  спор на ровном месте. **[решение]** — предупреждение обязательно.
- Валюты: список ограничен теми, что мы принимаем; недоступные валюты видны с
  причиной, а не отсутствуют.

### Фиксация курса и таймер ▣ C-12

Состояния котировки:

| Код | Условие | Интерфейс |
|---|---|---|
| `Q-01` `fresh` | Окно > 10 мин | Курс, «зафиксирован до {время, 2 зоны}», остаток крупно, но не мигает |
| `Q-02` `expiringSoon` | ≤ 10 мин | Усиление, обратный отсчёт мм:сс, кнопка «обновить курс» появляется заранее |
| `Q-03` `expired` | Окно истекло | Суммы в лари **скрыты**, не показаны устаревшими. Карточка: «курс истёк, обновите, чтобы увидеть актуальную сумму». Введённое в калькулятор сохраняется (WCAG 2.2.1) |
| `Q-04` `requoting` | Идёт запрос новой | Скелет вместо сумм, кнопка в состоянии «в работе» |
| `Q-05` `requoted` | Новая котировка | **Явное сравнение**: было / стало / разница. Молча не подменяем **[установлено]** |
| `Q-06` `unavailable` | Провайдер котировок недоступен | Реквизиты показываем, суммы в лари — нет; честно: курс определится при зачислении, вы увидите его до конвертации и сможете отказаться. **[открыто §8.7]** |
| `Q-07` `moneyArrivedAfterExpiry` | Деньги пришли, окно истекло | **Блокирующая карточка** на экране сделки: новая сумма, было/стало, два действия — принять новый курс или вернуть деньги на счёт-источник. До решения деньги не конвертируются и лежат в `M-05` |

Таймер: не единственный носитель информации — рядом абсолютное время. Истечение
не приводит к потере данных и не выполняет действий за пользователя.

### Реквизиты и референс ▣ C-16

- Каждое поле копируется отдельно одним нажатием; область нажатия — вся строка,
  не только иконка (≥44 px).
- Подтверждение копирования — короткое, не перекрывает поле.
- **Референс визуально выделен** сильнее остальных полей и вынесен в собственную
  рамку: это единственное, потеря чего стоит клиенту недели.
- Предупреждение `...reference.warning` — постоянное, не сворачиваемое: копи-бриф
  — перевод без референса не потеряется, но **задержится на ручной разбор**;
  назвать ожидаемый срок разбора. Пугать нельзя, скрывать нельзя.
- Формат референса: только латиница и цифры, без символов, неоднозначно
  читаемых (0/O, 1/I), группами по 4 **[решение]** — его переписывают руками в
  форму банка.
- «Отправить реквизиты на почту» — допустимо: письмо содержит реквизиты, но
  **не содержит ссылок на действия** (§1.6) и содержит блок безопасности.
  **[открыто §8.8]**: не противоречит ли это правилу «единственное место, где вы
  видите реквизиты — кабинет».
- Печать/PDF-версия платёжного поручения для похода в отделение банка — нужна
  сегменту, который платит из отделения **[решение]**, генерируется на языке
  клиента + английский дубль полей.
- Скриншот не запрещаем — запрещать бессмысленно; вместо этого водяной знак на
  PDF.

### Правило плательщика ▣ C-05b

**Показывается до перевода, а не после отказа** **[установлено]**.

- Позиция: **над** реквизитами, не под ними, не в подвале.
- Не сворачивается.
- Копи-бриф: отправитель обязан совпадать с вами; перевод от другого лица будет
  остановлен — **включая супруга, родителей и вашу компанию**, если не
  согласовано заранее; что делать, если иначе не получается (обратиться заранее);
  что происходит с остановленным платежом (не пропадает, разбирается, может быть
  возвращён отправителю).
- Чекбокс подтверждения `...payerRule.confirm` обязателен перед кнопкой «я
  отправил перевод». **[решение]** Чекбокс не закрывает доступ к реквизитам —
  реквизиты видны сразу; он подтверждает осведомлённость при заявлении перевода.
  Прятать реквизиты за чекбокс — лишнее трение на ровном месте.

### Статус зачисления ▣ C-15

```
┌──────────────────────────────────────┐
│ «buyer.topup.status.title»           │
│ ● «...declared» {дата}               │
│ ○ «...received»                      │
│ ○ «...credited»                      │
│ «...eta» {ожидаем до 2 сентября}     │
│ ▸ «...delayed.help»                  │
└──────────────────────────────────────┘
```

| Состояние | Что показываем |
|---|---|
| Не заявлен | Блок не показывается |
| Заявлен, ждём | Три шага, ожидаемый срок диапазоном (не точкой), «мы сообщим сами» |
| **Задержка** (срок вышел) | Спокойно, без тревоги: что бывает и почему (корреспондент, проверка банка, выходные в другой стране); что сделать — прислать подтверждение перевода из своего банка (кнопка загрузки); что делаем мы; когда следующее обновление. **Обязателен номер обращения** |
| Поступило, не опознано (`M-03`) | См. блок 1; здесь — действие «прислать подтверждение» |
| Поступило от третьего лица (`M-04`) | Объяснение + путь |
| Зачислено | Успех, сумма, переход к сделке; **если сумма отличается** — показываем разницу и следующий шаг (`M-07`/`M-08`) |
| Возвращено банком-корреспондентом | Отдельное состояние: деньги вернулись отправителю, причина, что делать |

### Способы пополнения ▣ C-31
Банковский перевод — по умолчанию и рекомендован. Pay-by-bank — где доступен
(доступность зависит от страны банка клиента; недоступность объясняется).
Карта — видна только при сумме ниже порога; выше порога **вариант присутствует в
списке с объяснением, почему недоступен** — иначе клиент ищет карту глазами и не
понимает, куда она делась. Копи-бриф: коротко и без извинений — на суммах такого
размера комиссия за карту несоразмерна.

### Состояния экрана

`SC-load` — скелет калькулятора, реквизиты не показываются до подтверждения
сервером (ошибочные реквизиты хуже отсутствующих).
`SC-error` при недоступности реквизитов — прямой текст: не показываем реквизиты
из кэша **никогда** **[решение]**, потому что подмена реквизитов — основной
вектор атаки, а кэш — это то, что могло устареть.
`SC-denied` — пополнение недоступно (сделка закрыта / комплаенс-стоп): причина,
куда обратиться.
`SC-frozen` — реквизиты скрыты, объяснение.

### Адаптивность
Калькулятор на `xs` — два поля вертикально с кнопкой переворота между ними; на
`md+` — в строку. Реквизиты — всегда список «метка / значение / копировать»
вертикально: таблица в две колонки на 360 px ломает IBAN. Липкая кнопка «я
отправил перевод» на `xs` не перекрывает последнее поле реквизитов.

### Доступность
Копирование сообщает результат в `aria-live`; таймер котировки — `polite`, за
минуту до конца — `assertive`; поля сумм с `inputmode="decimal"`; референс
объявляется по символам (`aria-label` с разделением), чтобы его можно было
записать со слуха.

### Компоненты
C-04, C-05, C-05b, C-11, C-12, C-13, C-14, C-15, C-16, C-31, C-32.

---

## 3.5 Экран B-05 — Мой счёт

### Назначение
Показать, что деньги остаются его собственными, пока не зарезервированы. Это
единственный экран, который делает разницу между «мы взяли ваши деньги» и «ваши
деньги на вашем счёте».

### Структура

```
┌──────────────────────────────────────┐
│ H1 «buyer.account.title»             │
│ ▣ C-04 Баланс                        │
│  «...available»   {217 000 ₾}        │
│  «...reserved»    {0 ₾}              │
│  ▸ «...otherCurrencies» {80 000 USD} │
│                                      │
│ [[ «buyer.account.withdraw» ]]       │
│  «buyer.account.withdraw.note»       │
│                                      │
│ «buyer.account.history»              │
│ ▣ C-17 Список движений               │
│  · {дата} «...op.topup»    +{сумма}  │
│  · {дата} «...op.fx»       ±{сумма}  │
│  · {дата} «...op.reserve»   {сумма}  │
│  · {дата} «...op.settle»   −{сумма}  │
└──────────────────────────────────────┘
```

### Состояния кнопки вывода — центральный элемент экрана

| Код | Условие | Что видит клиент |
|---|---|---|
| `W-01` | Средства свободны, счёт-источник известен | Кнопка активна. Подпись: вывод только на счёт-источник, на ваше имя |
| `W-02` | Часть зарезервирована | Кнопка активна на свободную часть; суммы разделены явно |
| `W-03` | **Зарезервировано** | Кнопка заменяется строкой «Средства зарезервированы до ⏱» + объяснение, почему нельзя, и когда снова будет можно **[установлено]**. Это не «серая кнопка», а другой элемент |
| `W-04` | Счёт-источник неизвестен (денег ещё не было) | Вывода нет, потому что выводить некуда и не на что; объяснение |
| `W-05` | Комплаенс-стоп | Недоступно, причина, номер обращения |
| `W-06` | Вывод в процессе | Статус, срок, невозможность отменить после отправки — сказано до подтверждения |
| `W-07` | Несколько счетов-источников | Выбор из них; **произвольные реквизиты ввести нельзя** (красная линия №9) |

Форма вывода: сумма (с «вывести всё»), счёт-источник (только выбор), подтверждение
вторым фактором, экран обзора перед отправкой (правило `form-design` для
высокорисковых форм), после отправки — статус, а не пустота.

### История движений ▣ C-17
Дата, тип операции (человеческим языком, не кодом счёта), сумма со знаком,
валюта, состояние, ссылка на сделку и на документ. Фильтр по типу и периоду.
Пустое состояние: «движений пока нет» + что появится после первого пополнения.
**Конвертация показывается двумя строками** (списание валюты, зачисление лари) с
курсом — одна строка со «спрятанным» спредом читается как обман.
Экспорт — CSV/PDF (маркирован водяным знаком).

### Крайние случаи
Отрицательный баланс невозможен по инварианту; если пришёл — показываем ошибку
данных и обращение, а не минус. Валюта, в которой мы больше не принимаем, —
остаток виден, пополнение недоступно с объяснением. Остаток ниже стоимости
перевода — предупреждение до вывода, что комиссия банка съест сумму.

### Адаптивность
`xs` — карточки движений (дата и сумма в одной строке, тип второй строкой), не
таблица. `md+` — таблица с фиксированной первой колонкой даты.

### Компоненты
C-04, C-05, C-14, C-17, C-25, C-30, C-34 (форма вывода).

---

## 3.6 Экран B-06 — Документы

### Назначение
Дать доступ к документам сделки и объяснить, что каждый просмотр фиксируется.

### Данные
Тип документа, сторона-источник, дата, состояние (действителен / заменён /
на проверке), доступность для роли.

### Структура
Список, сгруппированный по смыслу: по сделке · по объекту · мои документы
(проверка личности) · документы платформы (оферта, подтверждения, отчёты).
Каждый элемент: название, дата, размер, статус, действие «посмотреть».

### Поведение
- Просмотр — во встроенном просмотрщике **с водяным знаком** (идентификатор
  смотрящего + время) **[установлено]**. Скачивание, где разрешено, — с тем же
  знаком.
- Перед первым просмотром — однократное объяснение: просмотр фиксируется в
  журнале; это защищает вас и нас.
- Клиент видит **свой** журнал просмотров: когда он сам смотрел. Видит ли он, что
  документ смотрел кто-то ещё — **[открыто §8.9]**.
- Документы второй стороны, содержащие персональные данные, недоступны; в списке
  видны как «документ второй стороны — недоступен» с причиной, а не отсутствуют.

### Состояния
`SC-empty` — документов нет: что появится и когда.
`SC-load` — скелет списка; просмотрщик грузится с индикатором и возможностью
отмены (крупные PDF).
`SC-error` — файл не открылся: повтор, альтернатива (скачать), обращение.
`SC-denied` — нет прав на этот документ: причина, кто может выдать.
Файл повреждён / формат не поддерживается — отдельные состояния, не общая ошибка.

### Адаптивность
На `xs` просмотрщик — полноэкранный, с явной кнопкой возврата; зум жестами;
водяной знак масштабируется вместе с документом и остаётся читаемым.

### Компоненты
C-05, C-14, C-17, C-18 (просмотрщик), C-19 (бэдж статуса документа).

---

## 3.7 Экран B-07 — Безопасность и помощь

### Назначение
Сделать защиту действием, а не текстом. Экран, на который клиент приходит, когда
ему написали «мы сменили реквизиты» — и здесь он должен за десять секунд понять,
что это мошенничество.

### Структура

```
┌──────────────────────────────────────┐
│ H1 «buyer.security.title»            │
│ ▣ C-05 Блок безопасности (развёрнуто)│
│                                      │
│ «buyer.security.never.title»         │
│  · «...never.phone»                  │
│  · «...never.email»                  │
│  · «...never.messenger»              │
│  · «...never.otherAccount»           │
│                                      │
│ «buyer.security.verify.title»        │
│  «...verify.body» (как убедиться,    │
│   что письмо от нас)                 │
│                                      │
│ [[ «buyer.security.report» ]]        │
│  «...report.note»                    │
│                                      │
│ «buyer.security.sessions.title»      │
│  ▣ C-17: устройства, входы, страны   │
│  [ «...sessions.revokeAll» ]         │
│                                      │
│ «buyer.security.2fa.title»  [настроить]│
│                                      │
│ «buyer.help.title»                   │
│  ▸ вопросы по разделам               │
│  «...contact» {канал, часы работы,   │
│   срок ответа}                       │
└──────────────────────────────────────┘
```

### Поведение
- **«Сообщить о подозрительном контакте»** — главное действие экрана. Форма из
  трёх полей (канал, что произошло, файл/скриншот) + **немедленный эффект**:
  предложение временно приостановить операции по сделке. Приостановка — обратимая,
  снимается через оператора. Копи-бриф: подчеркнуть, что сообщение ничего не
  ломает и что лучше сообщить зря, чем не сообщить.
- Список сессий: устройство, страна, время, текущая помечена; отзыв всех, кроме
  текущей.
- История значимых действий по учётной записи: вход, смена контакта, вывод,
  заявление о переводе — видна клиенту.

### Состояния
`SC-empty` для сессий не бывает (текущая всегда есть). Отправка сообщения:
идёт / принято с номером обращения / не отправилось — с сохранением текста и
альтернативным контактом.

### Компоненты
C-05, C-14, C-17, C-25, C-32, C-35 (форма обращения).

---

# 4. Кабинет получателя

**Устройство:** desktop-first (`lg` базовый), с обязательной полной поддержкой
`xs` для двух экранов: подтверждение средств и реквизиты выплаты.
**Языки:** русский, английский, грузинский.
**Два режима одного кабинета** **[установлено]**: продавец-физлицо и застройщик.
Режим определяется типом контрагента, **не переключается пользователем**;
переключатель существует только у контрагента, у которого есть обе роли
(редкий случай, `SC` не проектируем в первой версии).

## 4.0 Каркас и навигация

```
┌────────────────────────────────────────────────────────────────┐
│ ▣ C-20  «payee.nav.brand»        {контрагент}  🌐  ⚙  выход    │
├───────────────┬────────────────────────────────────────────────┤
│ ▣ C-36        │                                                │
│ Боковая нав.  │            СОДЕРЖИМОЕ                          │
│               │                                                │
│ режим продавца│                                                │
│  · Сделка     │                                                │
│  · Реквизиты  │                                                │
│  · Документы  │                                                │
│  · Безопасность│                                               │
│               │                                                │
│ режим застройщика                                              │
│  · Дашборд    │                                                │
│  · Договоры   │                                                │
│  · Платежи ⑦  │  ← счётчик несопоставленных всегда в навигации │
│  · Выплаты    │                                                │
│  · Отчёты     │                                                │
│  · Реквизиты  │                                                │
│  · Безопасность│                                               │
├───────────────┴────────────────────────────────────────────────┤
│ ▣ C-05 Блок безопасности (постоянно)                           │
└────────────────────────────────────────────────────────────────┘
```

Счётчик несопоставленных платежей — **в навигации, а не только на дашборде**:
это ежедневная работа застройщика, и она не должна требовать похода на дашборд.

---

## 4.1 Экран R-01 — Подтверждение средств (главный экран продавца)

### Назначение
**Заменить продавцу владение деньгами.** От того, поверит ли он этой карточке,
зависит, пойдёт ли он к нотариусу отдавать квартиру. Это самый важный экран
кабинета получателя и, вероятно, всего продукта.

### Что делает карточку убедительной

Проектные принципы, из которых собрана анатомия:

1. **Названа сумма, названо место.** Не «средства обеспечены», а сумма + банк +
   тип счёта. Абстракция не убеждает.
2. **Назван момент.** Дата и время установления резерва с зоной — событие, а не
   состояние.
3. **Оба исхода показаны рядом, симметрично.** Продавец должен увидеть и плохой
   исход тоже — карточка, показывающая только хороший, читается как реклама.
4. **Названо, сколько он получит на руки**, а не сумма сделки. Вычеты построчно.
5. **Проверяемость.** У подтверждения есть номер и документ, который можно
   показать нотариусу и юристу и проверить у нас по номеру.
6. **Названо, чего мы НЕ обещаем.** ⚖ Безотзывное поручение ничтожно
   (`PRODUCT.md` §11), поэтому карточка не имеет права говорить «покупатель не
   сможет забрать деньги». Она говорит, что резерв установлен, окно короткое, и
   что при бездействии деньги вернутся покупателю. Честная граница — то, что
   отличает нас от обещания, которое нельзя исполнить.
7. **Тот же язык, что у покупателя.** Обе стороны видят одни и те же цифры и
   один и тот же дедлайн (`C-04`, `C-03`), иначе первый же спор будет о том, кто
   что видел.

### Данные
Уровень подтверждения `A0…A4`; сумма резерва; банк-держатель и тип счёта; момент
резервирования; номер подтверждения; сумма к выплате и вычеты; реквизиты
(маскированные) и их статус; дедлайн и последствие; состояние сделки; ссылка на
документ подтверждения.

### Структура (`lg`)

```
┌───────────────────────────────────────────────────────────────┐
│ ▣ C-08 Уровень   A0 ─ A1 ─ A2 ─[A3]─ A4                       │
├───────────────────────────────────────────────────────────────┤
│ ▣ C-37 КАРТОЧКА ПОДТВЕРЖДЕНИЯ СРЕДСТВ                         │
│                                                               │
│  H1 «payee.assurance.reserved.title»                          │
│                                                               │
│  {217 000 ₾}                                                  │
│  «payee.assurance.reserved.subtitle»                          │
│  «payee.assurance.reservedAt» {31 августа, 11:20 (Тбилиси)}   │
│  «payee.assurance.confirmationNo» {SD-CONF-7K42}   [⧉]        │
│                                                               │
│  ── «payee.assurance.whereMoney.title» ──────────────────     │
│  «payee.assurance.whereMoney.body»                            │
│   {банк} · «...nominalAccount» · «...notOnOurBalance»         │
│                                                               │
│  ── «payee.assurance.outcomes.title» ────────────────────     │
│  ┌────────────────────────┬────────────────────────┐          │
│  │ «...outcomes.registered│ «...outcomes.notReg    │          │
│  │  .title»               │  .title»               │          │
│  │ «...registered.body»   │ «...notReg.body»       │          │
│  │ → {214 395 ₾} {когда}  │ → «...moneyBackToBuyer»│          │
│  │                        │   «...canRetry»        │          │
│  └────────────────────────┴────────────────────────┘          │
│                                                               │
│  ── «payee.assurance.youReceive.title» ──────────────────     │
│  ▣ C-04  {214 395 ₾}                                          │
│    «...dealAmount» {217 000 ₾}                                │
│    «...fee»        −{2 605 ₾}   ▸ из чего                     │
│    «...payoutTo»   ▣ C-38 •••• 4417 · {имя} · ✓ проверено     │
│                                                               │
│  ── ⏱ ▣ C-03 {дедлайн, 2 зоны} ─────────────────────────      │
│    «common.deadline.consequence.registration»                 │
│                                                               │
│  ⚖ «payee.assurance.limits»  (чего мы не обещаем)             │
│                                                               │
│  [ «payee.assurance.download» ]  [ «payee.assurance.verify» ] │
└───────────────────────────────────────────────────────────────┘
│ ▣ C-06 Лента сделки (представление получателя)                │
│ ▣ C-07 Объект                                                 │
│ ▣ C-10 Стороны и документы                                    │
│ ▣ C-05 Блок безопасности                                      │
```

### Копи-брифы ключевых слотов ⚖

| Ключ | Что обязан сообщить | Чего не имеет права |
|---|---|---|
| `payee.assurance.reserved.title` | Факт: средства зарезервированы под эту сделку | Слов «гарантия», «эскроу», «заблокировано навсегда» |
| `...whereMoney.body` | Где деньги: счёт номинального держания в конкретном банке; не на балансе платформы. Вносящая сторона вправе отозвать средства до наступления условия, и получатель знает об этом заранее | **Утверждать или подразумевать, что деньги нельзя отозвать** |

⚠️ **Прежняя редакция просила сказать получателю неправду.** Формулировка «не у
покупателя в распоряжении» подразумевает безотзывность, а безотзывное поручение по
гражданскому кодексу **ничтожно**: средства до расчёта остаются собственностью
вносящей стороны и отзывны. Обещать продавцу обратное мы не вправе — и это же
записано в `CORE.md` Ф6, `SECURE-SETTLEMENT.md` §4 и `STATE-MACHINES.md` §1.4.

Что даём вместо безотзывности: подтверждение средств, **короткое окно** между
подачей и расчётом, немедленное уведомление об отзыве и договорную ответственность
платформы. Окно короткое именно поэтому, а не ради скорости.
| `...outcomes.registered.body` | Что при подтверждённой регистрации расчёт исполняется в тот же день, и он получит указанную сумму | Называть точный час без основания |
| `...outcomes.notReg.body` | Что при ненаступлении условия до срока резерв снимается, деньги остаются у покупателя, сделку можно провести заново | Обвинять сторону; обещать компенсацию |
| `...limits` ⚖ | Границу обещания: что именно подтверждено и на какой срок | Создавать впечатление безотзывности |
| `...verify` | Что подтверждение можно проверить по номеру | — |

### Состояния карточки

| Код | Уровень | Заголовок о чём | Что показано |
|---|---|---|---|
| `S-01` | `A0` | Сделка заведена | Условия, объект, что должно произойти до появления денег. **Сумм резерва нет** |
| `S-02` | `A1` | Покупатель проверен | То же + отметка о проверке покупателя (без его персональных данных) |
| `S-03` | `A2` | Средства у покупателя, ещё не зарезервированы | Сумма на счёте покупателя, **явно**: деньги пока его и отзывны; резерв будет установлен в день подачи |
| `S-04` | `A2` частично | Собрано {X} из {Y} | Прогресс сбора, срок добора, что будет, если не доберут |
| `S-05` | **`A3`** | **Средства подтверждены** | Полная карточка (выше) |
| `S-06` | `A3` | Документы поданы | То же + номер заявления, ожидание реестра, срок |
| `S-07` | `A3→A4` | Регистрация подтверждена, расчёт исполняется | Что деньги придут сегодня; ожидаемое время; реквизиты, куда |
| `S-08` | `A4` | Расчёт исполнен | Сумма, дата, реквизиты, документ; ссылка на выписку |
| `S-09` | `A3` | Поручение отправлено, банк не подтвердил (`M-13`) | Честно: подтверждение банка не получено, повторно **не отправляем**, проверим по выписке; когда сообщим. Копи-бриф: не паниковать и не обещать |
| `S-10` | `A2` | Условие не наступило, резерв снят | Что произошло, что деньги вернулись покупателю, что сделку можно провести заново, что уже сделано не пропало |
| `S-11` | — | Сделка отменена | Причина в допустимых пределах, что дальше |
| `S-12` | — | Реквизиты меняются, идёт охлаждение | **Выплата приостановлена**, срок охлаждения, кто уведомлён; если менял не он — крупное предупреждение и кнопка «это не я» |
| `S-13` | — | Операции приостановлены (комплаенс) | Что деньги на месте, идёт проверка, срок, контакт |
| `S-14` | — | Часы приостановлены | Таймер остановлен, причина (реестр недоступен), срок будет пересчитан |

### Состояния экрана
`SC-load` — скелет карточки; **уровень `A*` и сумма не показываются, пока не
подтверждены сервером**.
`SC-stale` — карточка помечена «данные на {время}», кнопка скачивания
подтверждения недоступна (нельзя выдать документ по устаревшим данным).
`SC-error` — не смогли показать: явно «это не значит, что с деньгами что-то не
так», повтор, контакт, номер обращения.
`SC-denied` — продавец больше не сторона сделки.
`SC-empty` — сделок нет: что произойдёт, когда его добавят.

### Крайние случаи
- Несколько сделок у одного продавца → список сделок с карточками-превью
  (уровень, сумма, дедлайн), вход в полную карточку.
- Сумма изменена оператором (пересогласование) → карточка показывает «было/
  стало» и дату изменения, а не тихо новое число.
- Продавец смотрит с телефона у нотариуса — см. адаптивность; это основной
  сценарий использования карточки, а не побочный.
- Продавцу показали поддельный кабинет: карточка содержит номер подтверждения,
  проверяемый на публичной странице проверки; блок безопасности объясняет, как
  проверить.
- Сделка с несколькими получателями (доли) → каждый видит **свою** сумму к
  получению и общую сумму сделки, но не реквизиты другого.

### Адаптивность

| Класс | Поведение |
|---|---|
| `xs` | Карточка вертикально; два исхода — **друг под другом, визуально равные по весу**, не «хороший крупно, плохой мелко». Сумма и уровень — первый экран |
| `md` | Два исхода в две колонки |
| `lg`+ | Как в макете; лента и объект — ниже, не сбоку: карточка не делит внимание |

### Доступность
Уровень `A*` — не только цветной прогресс: текст «уровень 3 из 4: средства
зарезервированы». Два исхода — семантически список, не таблица. Сумма к получению
— в заголовке уровня h2, чтобы находилась навигацией по заголовкам.

### Компоненты
C-03, C-04, C-05, C-06, C-07, C-08, C-10, C-13, C-14, C-37, C-38.

---

## 4.2 Экран R-02 — Реквизиты выплаты (защитный периметр)

### Назначение
**[установлено]** Подмена реквизитов — самый вероятный сценарий атаки; 86% ущерба
от компрометации деловой переписки идёт через безотзывные переводы. Экран
проектируется как защитный периметр, а не как форма. Здесь трение — функция, а не
дефект.

### Принципы экрана

1. **Имя владельца счёта не вводится вручную.** Оно берётся из проверки личности
   и показывается как **нередактируемое** значение с объяснением. Это сильнее,
   чем сверка после ввода: не даёт даже попытки **[решение]**.
2. Вход в раздел — под свежим вторым фактором (не старше 5 минут).
3. Все действия — с задержкой и уведомлением, ни одно не мгновенно.
4. Экран всегда показывает, **что сейчас заблокировано и почему**, а не только
   поля.
5. История изменений реквизитов видна владельцу кабинета всегда.

### Структура (`lg`)

```
┌───────────────────────────────────────────────────────────────┐
│ H1 «payee.requisites.title»                                   │
│ ▣ C-05 Блок безопасности (здесь постоянно, не в подвале)      │
│                                                               │
│ ▣ C-39 СОСТОЯНИЕ ПЕРИМЕТРА                                    │
│  ● «payee.requisites.state.<code>.title»                      │
│    «payee.requisites.state.<code>.body»                       │
│    ⏱ (если применимо)                                         │
│                                                               │
│ ── «payee.requisites.current» ──────────────────────────      │
│  «...holderName»  {Имя из проверки личности}  🔒               │
│      «...holderName.why»                                      │
│  «...iban»        {GE** **** **** 4417}                       │
│  «...bank»        {…}                                         │
│  «...verifiedAt»  {дата} · ▣ C-19 ✓ проверено                 │
│                                                               │
│  [ «payee.requisites.change» ]ᵈ  ← причина рядом, если нельзя  │
│                                                               │
│ ── «payee.requisites.history» ──────────────────────────      │
│  ▣ C-17 · {дата} · {что изменено} · {кто} · {как подтверждено}│
└───────────────────────────────────────────────────────────────┘
```

### Состояния периметра (`payee.requisites.state.*`)

| Код | Условие | Поля | Основное действие | Что говорит экран |
|---|---|---|---|---|
| `P-01` `empty` | Реквизиты не заданы | Форма ввода | Добавить | Что без реквизитов выплата невозможна; что имя фиксировано |
| `P-02` `entering` | Ввод под 2FA | Форма активна | Сохранить | Что после сохранения потребуется проверка |
| `P-03` `nameMismatch` | Имя владельца ≠ профиль | Форма заблокирована | нет | **Жёсткий стоп, не предупреждение** **[установлено]**: счёт должен быть на ваше имя; что делать (счёт на своё имя или обращение); никакой кнопки «всё равно сохранить» |
| `P-04` `testPending` | Тестовый перевод отправлен | Поле кода | Ввести код | Что мы отправили малую сумму с кодом в назначении; когда искать в выписке; сколько попыток |
| `P-05` `testFailed` | Код не сошёлся | Поле кода | Повтор / обращение | Сколько попыток осталось; после исчерпания — блок и эскалация |
| `P-06` `verified` | Проверено | Read-only | Изменить | Что проверено и когда |
| `P-07` `lockedByReserve` | Средства зарезервированы | Read-only | Запросить изменение | **[установлено]** Что реквизиты заблокированы, потому что под сделку зарезервированы средства; что изменение возможно, но запустит охлаждение и приостановит выплату |
| `P-08` `changeRequested` | Запрос изменения подан | Read-only + новые значения «ожидают» | Отменить запрос | Что запущено: повторная верификация, охлаждение ⏱, уведомление всех сторон, второе утверждение у нас; **выплата запрещена на этот срок** |
| `P-09` `cooling` | Идёт охлаждение | Read-only | Отменить изменение | Обратный отсчёт + **точная дата и время вступления в силу**; кто уведомлён (список каналов и сторон); что отмена возвращает старые реквизиты немедленно |
| `P-10` `blocked72h` | До расчёта < 72 ч | **Форма не открывается** | Обращение | **[установлено]** Автоматический блок: изменение реквизитов в последние 72 часа невозможно; эскалация создана автоматически, номер обращения виден; контакт |
| `P-11` `blockedCompliance` | Стоп комплаенса | Read-only | Обращение | Причина в допустимых пределах, срок |
| `P-12` `underReverify` | Идёт повторная верификация личности | Read-only | Продолжить проверку | Что нужно от него сейчас |

### Форма ввода — правила

- Поля: страна банка → банк (справочник, поиск) → IBAN/номер счёта → SWIFT (если
  требуется) → **имя владельца (нередактируемое, из KYC)**.
- IBAN: маска по стране, валидация контрольного разряда **до** отправки, вставка
  из буфера с очисткой пробелов, проверка соответствия страны и банка.
- Повторный ввод IBAN во второе поле для сверки **[решение]** — да, это раздражает,
  но цена опечатки здесь — потеря шестизначной суммы. Вставка во второе поле
  запрещена (иначе сверка бессмысленна).
- Перед сохранением — **экран обзора** с полным IBAN, именем и банком крупно и
  вопросом «это ваш счёт?». Правило `form-design` для высокорисковых форм.
- Второй фактор — на сохранении, не на входе в форму (на входе — проверка
  свежести сессии).

### Тестовый перевод с кодом ▣ C-40

**[установлено]** За сутки до расчёта мы отправляем тестовый перевод с кодом в
назначении; продавец называет код в кабинете. Проверяет и счёт, и владельца.

```
┌───────────────────────────────────────────┐
│ «payee.requisites.test.title»             │
│ «payee.requisites.test.howItWorks»        │
│  1 «...step.sent»    {дата, сумма}        │
│  2 «...step.find»    (где искать в выписке)│
│  3 «...step.enter»                        │
│                                           │
│  [ код: _ _ _ _ _ _ ]                     │
│  «...attemptsLeft» {2}                    │
│  [[ «...submit» ]]                        │
│  ▸ «...notArrived»                        │
└───────────────────────────────────────────┘
```

Состояния: `не начат` · `отправлен, ждём зачисления в его банке` (с ожидаемым
сроком — 1 банковский день) · `ждём код` · `код неверен` (счётчик, лимит 3,
затем блок и эскалация; **счётчик виден с первой ошибки**) · `подтверждено` ·
`перевод вернулся` (счёт не существует или закрыт — реквизиты сбрасываются в
`P-01` с объяснением).
Копи-бриф `...notArrived`: что делать, если перевод не виден — сроки, что мы
проверим, что это не отменяет сделку.
**[открыто §8.10]** Сумма тестового перевода, за чей счёт, возвращается ли.

### Уведомления при изменении
При переходе в `P-08` уведомление уходит **всем сторонам по всем каналам**
**[установлено]**. В кабинете покупателя появляется баннер: реквизиты получателя
меняются, расчёт приостановлен до {дата}, это защитная мера. В кабинете продавца
— строка «кто уведомлён», чтобы он понимал масштаб происходящего и не пытался
«по-тихому» поменять счёт.
На всех экранах, где показано изменение, есть действие **«это не я»** — оно
немедленно отменяет запрос и создаёт инцидент.

### Состояния экрана
`SC-load` — скелет, значения не показываются; `SC-denied` — роль не имеет права
видеть реквизиты (например, сотрудник застройщика без полномочий): объясняем, кто
имеет; `SC-error` — не загрузились: **не показываем кэш** (см. §3.4).

### Адаптивность
`xs` — форма вертикально, поле IBAN на всю ширину, моноширинный ввод группами по
4; экран обзора — полноэкранный; блок безопасности над формой.

### Доступность
Нередактируемое поле имени — `readonly` с объяснением рядом, а не `disabled` (его
пропускает скринридер). Счётчик попыток — `aria-live`. Обратный отсчёт охлаждения
дублируется абсолютной датой.

### Компоненты
C-03, C-05, C-13, C-14, C-17, C-19, C-24, C-25, C-38, C-39, C-40.

---

## 4.3 Экраны R-03 — Документы и R-04 — Безопасность (продавец)

Совпадают с B-06 и B-07 по структуре и состояниям, с двумя отличиями:
- в списке документов есть раздел «что нужно от вас» с требованиями к подаче на
  регистрацию (аналог C-28);
- на экране безопасности добавлен пункт о реквизитах: единственное место
  изменения — кабинет, и любое изменение уведомляет все стороны.

---

## 4.4 Экран R-10 — Дашборд застройщика

### Назначение
Утренний экран: сколько пришло, сколько ждём, что просрочено, **что требует
разбора сегодня**.

### Структура (`lg`)

```
┌───────────────────────────────────────────────────────────────┐
│ H1 «payee.dev.dashboard.title»   [период ▾] [проект ▾]        │
│                                                               │
│ ▣ C-41 Метрики                                                │
│ ┌──────────┬──────────┬──────────┬──────────┬──────────┐      │
│ │«...recei-│«...expec-│«...over- │«...activ-│«...unmat-│      │
│ │ ved»     │ ted»     │ due»     │ eContr»  │ ched» ⑦  │      │
│ │{сумма}   │{сумма}   │{сумма}   │{N}       │{N}       │      │
│ │{Δ период}│{до конца │{N догов.}│          │{самый    │      │
│ │          │ месяца}  │          │          │ старый:  │      │
│ │          │          │          │          │ {3 дня}} │      │
│ └──────────┴──────────┴──────────┴──────────┴──────────┘      │
│                                                               │
│ ⚠ ▣ C-42 «payee.dev.unmatched.title»  ← НЕ в фильтре          │
│  ····  {дата} {сумма} {отправитель} [ Сопоставить ]           │
│  ····  {дата} {сумма} {отправитель} [ Сопоставить ]           │
│  [ «...seeAll» {7} ]                                          │
│                                                               │
│ ▣ C-43 «payee.dev.upcoming.title»   (ближайшие платежи)       │
│ ▣ C-43 «payee.dev.overdue.title»    (просрочка по возрасту)   │
│ ▣ C-05                                                        │
└───────────────────────────────────────────────────────────────┘
```

**Несопоставленные — на дашборде списком, а не числом** **[установлено]**: это его
ежедневная работа. Число в метриках дублирует, но не заменяет список.

### Данные
Поступления за период (сумма, количество); ожидаемые до конца месяца; просрочка с
возрастом; активные договоры; несопоставленные платежи с возрастом самого старого.

### Состояния
`SC-empty` — новый застройщик: вместо нулей — что сделать сначала (импортировать
договоры, добавить реквизиты, проверить, что уведомления настроены). Пустой
дашборд как список задач, а не как отчёт с нулями.
`SC-partial` — метрики пришли, лента нет (или наоборот).
`SC-stale` — «данные на {время}», суммы помечены.
`SC-denied` — роль сотрудника без прав на суммы: метрики скрыты, задачи видны.

### Крайние случаи
Несколько проектов/юрлиц — фильтр по проекту, суммы никогда не смешиваются между
юрлицами без явного «все». Валюты — если поступления в разных валютах, метрика не
складывается в одно число, а показывается по валютам.

### Адаптивность
`lg+` — 5 метрик в строку; `md` — 3+2; `xs` — вертикальный список метрик,
несопоставленные первым блоком (на телефоне застройщик заходит именно за этим).

### Компоненты
C-04, C-05, C-13, C-41, C-42, C-43.

---

## 4.5 Экран R-11 — Договоры и юниты

### Назначение
Реестр объектов и договоров, импорт таблицей, карточка договора с графиком.

### Структура
Таблица ▣ C-44: юнит · договор · покупатель · сумма · оплачено · остаток ·
ближайший платёж · статус. Фильтры-чипы: статус, проект, просрочка, период.
Поиск по номеру договора, юниту, имени покупателя (по трём алфавитам, §5.4).

### Импорт таблицей — мастер ▣ C-45

| Шаг | Содержание | Состояния |
|---|---|---|
| 1 Загрузка | Файл, шаблон для скачивания, лимиты названы заранее | ошибка формата, слишком большой, пустой |
| 2 Сопоставление колонок | Наши поля ↔ его колонки, автоугадывание, предпросмотр первых строк | обязательное поле не сопоставлено — переход заблокирован с указанием какого |
| 3 Проверка | **Построчные ошибки и предупреждения** с номером строки и причиной; счётчики «принято / с ошибками / дубли» | 0 валидных строк; дубли; конфликт с существующими договорами |
| 4 Предпросмотр | Что именно будет создано и что обновлено | — |
| 5 Импорт | Прогресс, возможность уйти со страницы, уведомление о завершении | частичный успех — **отчёт с CSV непринятых строк для исправления** |

Импорт **никогда не молчаливо частичный**: результат всегда показан как «принято
N, отклонено M», с выгрузкой отклонённых.

### Карточка договора
Стороны, юнит, сумма, график платежей (таблица: номер, дата, сумма, статус,
фактическая дата, документ), история изменений, документы, действия (выгрузить,
запросить изменение).

### Состояния
`SC-empty` реестра — предложение импорта и ссылка на шаблон.
`SC-empty` результата фильтра — **отличается** от пустого реестра: «под фильтр
ничего не попало» + сброс фильтра.
`SC-error` импорта — файл сохраняется, шаг не теряется.

### Адаптивность
`xl` — все колонки; `lg` — приоритет+: скрываются «остаток» и «проект», доступны
в детали; `md` — карточки вместо строк; `xs` — карточки, поиск сверху, фильтры в
выдвижной панели. Суммы и даты не обрезаются никогда (§1.1).

### Компоненты
C-14, C-17, C-19, C-44, C-45, C-46 (фильтры), C-47 (пустое состояние).

---

## 4.6 Экран R-12 — Платежи и очередь несопоставленных

### Назначение
Лента входящих с автоматическим сопоставлением и **рабочая очередь
несопоставленных на виду**.

### Структура

```
┌───────────────────────────────────────────────────────────────┐
│ H1 «payee.dev.payments.title»                                 │
│ [ Все ] [ ⚠ Несопоставленные ⑦ ] [ Сопоставленные ] [ Возвраты]│
│                       ↑ вкладка по умолчанию, если очередь > 0 │
│ ▣ C-44                                                        │
│ ····{дата} {сумма} {отправитель} {референс} ▣C-48 {уверенность}│
│      [ Сопоставить ]                                          │
└───────────────────────────────────────────────────────────────┘
```

**[решение]** Если очередь несопоставленных не пуста — она открывается по
умолчанию. Открывать «все платежи», когда есть незакрытая работа, — прятать её.

### Разбор несопоставленного — панель ▣ C-49

```
┌─────────────────────────────┬─────────────────────────────────┐
│ ПЛАТЁЖ                      │ КАНДИДАТЫ                       │
│ {дата} {сумма} {валюта}     │ ◉ {договор} {юнит} {покупатель} │
│ Отправитель: {имя}          │   ▣ C-48 совпадение 82%          │
│ Назначение: {текст}         │   ✓ сумма  ✓ имя  ✗ референс     │
│ Референс: {не распознан}    │ ○ {договор} … 64%               │
│ ▸ полные данные платежа     │ ○ …                             │
│                             │ [поиск по договорам]            │
│                             │ [[«...proposeMatch»]]           │
│                             │ [ «...notOurs» ] [ «...ask» ]   │
└─────────────────────────────┴─────────────────────────────────┘
```

- Кандидаты ранжированы, у каждого — **почему он кандидат** (совпало/не совпало
  по полям), не только процент.
- **[открыто §8.3]** Является ли действие застройщика окончательным
  сопоставлением или предложением, которое подтверждает оператор. Спроектировано
  как **предложение**: `«...proposeMatch»`, после которого платёж переходит в
  «на подтверждении». Причина: веб-слой не двигает деньги, а сопоставление —
  основание зачисления. Если владелец решит иначе, меняется только копия ключа и
  состояние.
- «Это не наш платёж» — отправляет в наш разбор с комментарием.
- «Запросить уточнение» — запрос плательщику через нас, не напрямую (канал
  прямой переписки не создаём — §7 `CABINETS.md`).

### Состояния
`SC-empty` очереди — **хорошее состояние**: «несопоставленных нет» + когда
последний раз были + среднее время разбора. Пустая очередь должна выглядеть как
достижение, иначе люди не верят, что она работает.
Платёж уже разобран другим сотрудником — блокировка с указанием кто и когда.
Платёж от третьего лица — отдельная пометка и объяснение, что он удержан у нас.
Частичная сумма — предложение сопоставить как частичный платёж по графику.

### Адаптивность
`lg+` — две панели; `md` — панель кандидатов под платежом; `xs` — пошагово:
платёж → кандидаты → подтверждение.

### Компоненты
C-14, C-17, C-19, C-44, C-46, C-48, C-49.

---

## 4.7 Экран R-13 — Выплаты (застройщик)

### Назначение
Что и когда ушло на его счёт, с реестром для бухгалтерии.

### Данные
Дата, сумма, состав (какие платежи вошли), удержанная комиссия, состояние,
референс банка, документ.

### Структура
Таблица выплат; раскрытие строки → состав выплаты по платежам и договорам.
Кнопка выгрузки реестра. Сверху — ожидаемая ближайшая выплата с датой.

### Состояния
Выплата в состоянии «неизвестно» (`M-13`) — **показывается** с честной
формулировкой: поручение отправлено, подтверждение банка не получено, повтор не
производится, проверим по выписке. Скрывать нельзя: он всё равно увидит по своему
счёту, что денег нет.
Выплата отклонена банком — причина в понятных терминах, что делаем дальше, нужно
ли что-то от него (обычно да: проверить реквизиты).
`SC-empty` — выплат ещё не было: когда будет первая.

### Компоненты
C-04, C-14, C-17, C-19, C-44.

---

## 4.8 Экран R-14 — Отчёты

### Назначение
Выгрузка в формате, который принимает его учётная система **[установлено]**.

### Структура
Выбор отчёта (поступления, начислено против удержано, выплаты, договоры,
просрочка) → период → формат (CSV, XLSX, формат учётной системы **[открыто
§8.11]** — какой именно) → генерация.

### Поведение — длинная операция
Генерация может занимать минуты: паттерн фоновой задачи. Немедленный отклик
(«отчёт готовится»), возможность уйти со страницы, уведомление о готовности,
список последних отчётов со сроком хранения ссылок. Прогресс — определённый, если
известно количество строк; иначе — этапы, а не бесконечный спиннер.

### Состояния
Готовится · готов (ссылка со сроком жизни) · пустой результат (период без данных
— отдельное состояние, не ошибка) · ошибка генерации (повтор, обращение) ·
слишком большой период (лимит назван заранее, предложение разбить).

### Компоненты
C-14, C-17, C-25, C-46, C-50 (фоновая задача).

---

# 5. Консоль операций

**Устройство:** desktop-first, минимум `lg`. Исключения, обязанные работать на
телефоне: экран утверждения выплаты (O-05) и дежурный дашборд (O-09).
**Языки:** русский и английский **[установлено]**, без грузинского.
**[открыто §8.12]** Если оператор грузиноязычный — решение о третьем языке
консоли принимается отдельно; архитектурно это ничего не меняет.

**Половина работы продукта.** Проектируется наравне с клиентскими кабинетами.

## 5.0 Каркас, роли и принцип видимости

```
┌────────────────────────────────────────────────────────────────────┐
│ ▣ C-20  «ops.nav.brand»   [поиск: сделка/сторона/платёж]  {роль} 🌐│
├──────────────┬─────────────────────────────────────────────────────┤
│ ▣ C-36       │ ▣ C-13 глобальные баннеры                           │
│ · Очередь ㉔ │  ⚠ покрытие нарушено · приём остановлен             │
│ · Сделки     │  ⚠ реестр недоступен · часы приостановлены          │
│ · Комплаенс ⑤│                                                     │
│ · Оракул     │            СОДЕРЖИМОЕ                               │
│ · Выплаты ③  │                                                     │
│ · Деньги     │                                                     │
│ · Аудит      │                                                     │
│ · Политики   │                                                     │
└──────────────┴─────────────────────────────────────────────────────┘
```

### Роли и что они видят **[установлено]** (`CABINETS.md` §1)

| Роль | Может | Не может |
|---|---|---|
| Оператор | Вести сделку, разбирать исключения, готовить выплату | Утверждать выплату, которую готовил |
| Комплаенс-офицер | Решения по проверке, источнику, санкциям | Утверждать выплату по сделке, где принимал решение по источнику **[решение]** |
| Финансовый контролёр | Сверка, покрытие, утверждение выплат | Менять реквизиты, готовить операцию |
| Руководитель операций | Второе утверждение, снятие блокировок | Утверждать в одиночку то, что требует двоих |
| Наблюдатель | Чтение, аудит | Любое действие |

### Принцип видимости запрета — **[решение]**

Недоступное действие **показывается с причиной**, а не скрывается. Скрытая кнопка
учит оператора, что «этого нет»; видимая с причиной учит, как устроен контроль.

```
[ «ops.payout.approve» ]ᵈ   ⛔ «ops.denied.samePreparer»
```

Исключения, где действие скрывается полностью: наблюдателю не показываются
действия вовсе (иначе интерфейс превращается в поле недоступных кнопок); и
операции, само наличие которых раскрывает чувствительный факт (например,
существование санкционного хита по стороне, к которой у роли нет доступа).

**Источник истины — сервер.** Клиент рисует ровно то, что пришло в
`allowedActions` / `deniedReasons`. Клиентская проверка роли — только для
отрисовки; она никогда не единственная защита.

---

## 5.1 Экран O-01 — Очередь работы (главный)

### Назначение
**Не «все сделки», а «что требует меня сейчас»** **[установлено]**. Оператор не
должен искать работу.

### Данные задачи
Тип; сделка; сумма; дедлайн и последствие; возраст; приоритет (срочность × сумма);
назначено на; состояние (свободна / взята / заблокирована); одно основное действие.

### Структура (`lg`)

```
┌────────────────────────────────────────────────────────────────────┐
│ H1 «ops.queue.title»                                               │
│ [ Мои ⑥ ] [ Свободные ⑱ ] [ Все ㉔ ]      [сорт: срочность ▾]      │
│ Чипы типов: [проверка ④][источник ②][санкции ①][платёж ⑦]          │
│             [регистрация ③][выплата ③][сверка ②][блокировка ②]     │
│                                                                    │
│ ▣ C-51 КАРТОЧКА ЗАДАЧИ                                             │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ ▣C-52 «ops.task.type.approvePayout»      ⏱ {через 2 ч 10 мин}  │ │
│ │ {SD-7K42} · {объект/адрес}                                     │ │
│ │ ▣ C-04 {213 495 ₾}                                             │ │
│ │ «ops.task.deadline.consequence.<x>»                            │ │
│ │ возраст {18 мин} · {взял: —}                                   │ │
│ │                                  [[ «ops.task.action.open» ]]  │ │
│ └────────────────────────────────────────────────────────────────┘ │
│ ····                                                               │
└────────────────────────────────────────────────────────────────────┘
```

**Одно основное действие на карточку** **[установлено]**. Всё остальное — в
меню «ещё» (передать, отложить с причиной, посмотреть сделку).

### Типы задач **[установлено]** и их основное действие

| Тип | Ключ | Основное действие | Экран |
|---|---|---|---|
| Проверить клиента | `ops.task.type.verifyClient` | Открыть проверку | O-03 |
| Разобрать источник средств | `...reviewSof` | Открыть досье | O-03 |
| Разобрать санкционный хит | `...reviewSanction` | Открыть хит | O-03 |
| Сопоставить платёж | `...matchPayment` | Открыть разбор | O-06 |
| Подтвердить регистрацию | `...confirmRegistration` | Открыть оракул | O-04 |
| Утвердить выплату | `...approvePayout` | Открыть утверждение | O-05 |
| Разобрать расхождение сверки | `...reviewBreak` | Открыть расхождение | O-06 |
| Снять блокировку | `...releaseBlock` | Открыть блокировку | O-02 |

### Поведение
- **Сортировка по умолчанию**: дедлайн, затем сумма. Оператор не настраивает
  приоритет — приоритет считает система; ручная сортировка доступна, но
  сбрасывается при перезаходе **[решение]**.
- **Взятие в работу** (claim): задача помечается за оператором на N минут, чтобы
  двое не делали одно и то же. Состояние `взята другим` — карточка видна, действие
  недоступно с указанием кто и с какого времени.
- Отложить — **только с причиной и сроком**, причина из перечня. Задача не
  исчезает, а получает новое время всплытия.
- Просроченная задача поднимается наверх и меняет вид (▣ C-03 состояние «срок
  наступил»), но **не удаляется и не скрывается**.
- Массовых действий нет **[решение]**: каждая задача — решение с последствиями
  для денег.

### Состояния экрана

| Код | Поведение |
|---|---|
| `SC-empty` | **Очередь пуста — это хорошее состояние.** Показываем: что покрыто, ближайший дедлайн по всем сделкам, метрики дежурного дашборда. Не пустой белый экран |
| `SC-empty` фильтра | «Под фильтр ничего не попало» + сброс, отличается от пустой очереди |
| `SC-load` | Скелет 3–5 карточек; счётчики в навигации — после загрузки, не нулями |
| `SC-partial` | Часть типов не загрузилась: показываем, каких типов не хватает, а не молчим |
| `SC-error` | Очередь не загрузилась: повтор, ссылка на поиск по сделкам как обходной путь |
| `SC-denied` | Роль не видит некоторых типов: показываем, что список отфильтрован по роли (иначе оператор думает, что задач нет) |
| `coverageBreach` | Верхний баннер + типы задач, связанные с приёмом новых сделок, помечены как приостановленные |

### Адаптивность
`xl` — две колонки карточек; `lg` — одна; `md` — одна, компактные карточки;
ниже `md` — заглушка, кроме задач типа «утвердить выплату», которые открываются
на телефоне.

### Доступность
Карточка — не кликабельный div: заголовок задачи — ссылка, основное действие —
кнопка. Обновление счётчиков — `aria-live=polite`. Появление новой срочной задачи
не перехватывает фокус.

### Компоненты
C-03, C-04, C-13, C-14, C-46, C-51, C-52.

---

## 5.2 Экран O-02 — Карточка сделки

### Назначение
**Полное состояние в одном месте** **[установлено]**: стороны с уровнем проверки,
объект с данными реестра, деньги с проводками, наблюдения оракула с уровнями
доверия, документы, полный лог решений с авторами и временем.

### Структура (`lg`)

```
┌─────────────┬───────────────────────────────────┬────────────────┐
│ ▣ C-53      │ {SD-7K42} · ▣C-19 {состояние}     │ ЛИПКАЯ ПАНЕЛЬ  │
│ Разделы:    │                                    │ ▣ C-04 Деньги  │
│ · Обзор     │  ... содержимое раздела ...        │  сделка/счёт/  │
│ · Стороны   │                                    │  резерв/комиссия│
│ · Объект    │                                    │ ▣ C-03 ⏱       │
│ · Деньги    │                                    │  + последствие │
│ · Оракул    │                                    │ ─────────────  │
│ · Документы │                                    │ Действия:      │
│ · Лог       │                                    │ [ … ]ᵈ причины │
└─────────────┴───────────────────────────────────┴────────────────┘
```

Панель денег и дедлайна **липкая и всегда видна** — оператор не должен скроллить,
чтобы вспомнить сумму и срок.

### Разделы

**Обзор** — состояние сделки и транша (техническое + человеческое), `M-*` код,
чек-лист готовности к следующему шагу с указанием, чего не хватает.

**Стороны** — для каждой: роль, имя в трёх представлениях (как в документе, как в
реестре, каноническое — `FUNCTIONAL.md` §5.3), уровень проверки, санкционный
статус с датой скрининга, источник средств с покрытием, реквизиты (маскированные,
с датой блокировки), история изменений.

**Объект** — данные реестра и договора рядом, поле к полю, с явным вердиктом по
каждому полю (совпало / расхождение / нет данных), дата выписки, обременения.

**Деньги** — остатки по счетам транша, **список проводок** (журнал, дата, счета,
суммы, сумма журнала = 0 как видимая проверка), комиссия начислено/удержано,
курс и спред. Проводки — только чтение, всегда.

**Оракул** — все наблюдения с уровнями доверия ▣ C-54, сводка сверки пяти полей.

**Документы** — с журналом доступа: кто смотрел и когда **[установлено]**.

**Лог решений** — кто, что, когда, на основании какой версии политики, с какого
устройства. Записи неизменяемы; «исправление» создаёт новую запись со ссылкой на
предыдущую **[установлено]** — в интерфейсе это кнопка «внести исправление» на
записи, а не «редактировать».

### Действия и запреты
Все действия — в правой панели, сгруппированы, каждое недоступное — с причиной.
Действия, меняющие деньги, **всегда** требуют: выбора основания из перечня,
текстового обоснования и подтверждения. Ни одно денежное действие не выполняется
по одиночному клику.

### Состояния
`SC-load` — каркас разделов, панель денег последней (сумма не показывается, пока
не подтверждена).
`SC-partial` — раздел не загрузился: локальная ошибка внутри раздела.
`SC-denied` — раздел недоступен роли: видно, что раздел существует, и кто имеет
доступ.
`SC-frozen` — сделка приостановлена: действия недоступны, причина и кто поставил.
Сделка удалена/объединена — не бывает; есть «отменена» и «заменена» со ссылкой.

### Адаптивность
`xl` — три колонки; `lg` — навигация разделов сворачивается в горизонтальные
табы, панель денег уходит наверх и становится липкой полосой; `md` — одна
колонка, панель денег — липкая полоса с раскрытием.

### Компоненты
C-03, C-04, C-06 (техническое представление), C-07, C-14, C-17, C-19, C-53, C-54,
C-55 (лог), C-56 (сравнение полей).

---

## 5.3 Экран O-03 — Комплаенс

### Назначение
Очередь проверок по уровням риска, разбор санкционных хитов, досье источника
средств.

### 5.3.1 Очередь проверок
Таблица: сторона · сделка · сумма · уровень риска · сработавшие детекторы (без
раскрытия логики порогов) · возраст · SLA · назначено. Сортировка по риску и
возрасту. Норматив решения ≤4 рабочих часов (`PRODUCT.md` §13) виден как
SLA-индикатор на каждой строке.

### 5.3.2 Разбор санкционного хита ▣ C-56

```
┌────────────────────────────────┬──────────────────────────────────┐
│ ПРОФИЛЬ                        │ ЗАПИСЬ ПЕРЕЧНЯ                   │
│ имя (док)     {…}              │ имя           {…}   схожесть 87% │
│ имя (канон)   {…}              │ псевдонимы    {…}                │
│ дата рожд.    {…}              │ дата рожд.    {…}   ✓ совпало    │
│ гражданство   {…}              │ гражданство   {…}   ✗ разошлось  │
│ документ №    {…}              │ документ №    {—}   нет данных   │
│                                │ перечень {OFAC} · {дата} · ссылка│
├────────────────────────────────┴──────────────────────────────────┤
│ РЕШЕНИЕ (обязательно)                                             │
│  ( ) «ops.sanction.decision.falsePositive»                        │
│  ( ) «ops.sanction.decision.match»                                │
│  ( ) «ops.sanction.decision.needMoreInfo»                         │
│  Обоснование: [ обязательное поле, минимум N символов ]           │
│  Версия политики: {v12} (фиксируется автоматически)               │
│  [[ «ops.sanction.submit» ]]                                      │
└───────────────────────────────────────────────────────────────────┘
```

- **Закрыть хит без решения нельзя** **[установлено]**. Попытка уйти — диалог с
  предупреждением, черновик обоснования сохраняется.
- Санкционная политика строже грузинской (`PRODUCT.md` §10) — на экране видно,
  какие перечни применяются и в какой редакции.
- Решение «совпадение» → немедленная блокировка стороны и сделки, экран
  показывает, что именно остановлено и какие уведомления ушли.

### 5.3.3 Досье источника средств
Чек-лист покрытия: **подтверждённое происхождение закрывает ≥90% суммы** и
связывает имя покупателя с движением денег **[установлено]**.

```
▣ C-09 Покрытие  ▓▓▓▓▓▓▓▓▓░  92%  ✓ порог пройден
 ┌ источник ──────────┬ сумма ─┬ документы ─┬ вердикт ──┐
 │ продажа недвижимости│120 000 │ 3/3 ✓      │ принято   │
 │ трудовой доход      │ 60 000 │ 2/3 ⚠      │ частично  │
 │ накопления          │ 20 000 │ 0/2 ✗      │ не принято│
 └─────────────────────┴────────┴────────────┴───────────┘
 ▣ C-57 Связка имени с движением денег:  ✓ / ✗ / нет данных
```

Связка имени — **отдельный явный индикатор**, а не строка в чек-листе: без неё
досье формально полное и практически бесполезное.
Каждый документ открывается с водяным знаком, просмотр логируется.
Решение по досье — принято / нужны уточнения (с формированием списка для клиента,
который попадёт в его кабинет как §3.2 `S-04`) / отклонено, с обоснованием.

### Состояния
`SC-empty` очереди — хорошее состояние с метриками.
`SC-denied` — оператор без роли комплаенса видит существование очереди и счётчик,
но не содержимое.
Документ не открывается — не блокирует решение по другим документам, но
запрещает вердикт «принято» по этому пункту.

### Компоненты
C-09, C-14, C-17, C-19, C-44, C-46, C-56, C-57, C-58 (форма решения).

---

## 5.4 Экран O-04 — Оракул

### Назначение
**[установлено]** Заявления в реестре и их статусы; заказ платной выписки с
показом стоимости; результат сверки пяти полей; ручное подтверждение с
обязательным приложением документа; **отдельный явный индикатор совпадения
собственника с профилем продавца**.

### Данные
Заявление (номер, дата, статус); наблюдения с уровнями доверия L1–L5; выписка
(дата, стоимость, файл); пять полей сверки; профиль продавца (личный номер,
документ, имена в трёх представлениях); пороги по сумме.

### Структура

```
┌────────────────────────────────────────────────────────────────────┐
│ H1 «ops.oracle.title» · {SD-7K42} · ▣C-04 {213 495 ₾}              │
│                                                                    │
│ ▣ C-59 ПОРОГИ ПО СУММЕ                                             │
│  «ops.oracle.threshold.band» {$25–100k}                            │
│  требуется: 2 источника · четыре глаза · 48 ч · микро-транзакция   │
│  выполнено: ●●○○                                                   │
│                                                                    │
│ ▣ C-54 НАБЛЮДЕНИЯ                                                  │
│ ┌──────┬──────────────────┬──────────┬───────────┬──────────────┐  │
│ │ L1   │ карточка заявления│{дата}   │ автомат   │ «...trust.L1»│  │
│ │ L3   │ платная выписка   │{дата}   │ {оператор}│ «...trust.L3»│  │
│ └──────┴──────────────────┴──────────┴───────────┴──────────────┘  │
│  «ops.oracle.statusMeansNothing» ⚠  (статус «завершено» ≠ переход) │
│                                                                    │
│  [ «ops.oracle.orderExtract» ] «...cost» {30 ₾} — списывается      │
│                                                                    │
│ ▣ C-56 СВЕРКА ПЯТИ ПОЛЕЙ                                           │
│ ┌─────────────────┬──────────────┬──────────────┬────────────┐     │
│ │ поле            │ ожидалось    │ в выписке    │ вердикт    │     │
│ ├─────────────────┼──────────────┼──────────────┼────────────┤     │
│ │ кадастровый код │ {…}          │ {…}          │ ✓          │     │
│ │ собственник     │ {…}          │ {…}          │ ✓          │     │
│ │ доля            │ {1/1}        │ {1/1}        │ ✓          │     │
│ │ основание записи│ {…}          │ {…}          │ ✓          │     │
│ │ обременения     │ {нет}        │ {нет новых}  │ ✓          │     │
│ └─────────────────┴──────────────┴──────────────┴────────────┘     │
│                                                                    │
│ ▣ C-57 СОВПАДЕНИЕ СОБСТВЕННИКА С ПРОФИЛЕМ ПРОДАВЦА                 │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │  ✓ «ops.oracle.ownerMatch.matched»                             │ │
│ │  основание: личный номер {01001•••••}  ← первичный признак      │ │
│ │  имя в реестре (ka): {…}                                       │ │
│ │  имя в документе (lat): {…}       схожесть {0.82} — вторичный  │ │
│ │  ▣ C-19 «...nameIsSecondary»                                   │ │
│ └────────────────────────────────────────────────────────────────┘ │
│                                                                    │
│ [ «ops.oracle.manualConfirm» ]ᵈ  требует приложенного документа    │
└────────────────────────────────────────────────────────────────────┘
```

### Индикатор совпадения собственника ▣ C-57 — отдельная спецификация

**[установлено]** «Без неё весь оракул бессмысленен» (`CABINETS.md` §5.2).
**[установлено]** Сверка ведётся по личному номеру или номеру документа, **не по
имени**; имя — вторичный сигнал (`FUNCTIONAL.md` §5.3).

Состояния индикатора:

| Код | Условие | Вид | Влияние |
|---|---|---|---|
| `OM-01` | Совпадение по личному номеру | ✓ подтверждено, основание названо | Разрешает продолжение |
| `OM-02` | Совпадение по номеру документа | ✓ подтверждено, основание названо | Разрешает |
| `OM-03` | Идентификатор не совпал | ✗ **жёсткий стоп**, крупно | Блокирует выплату полностью |
| `OM-04` | Идентификатора нет в выписке | ⚠ невозможно подтвердить автоматически | Блокирует до ручного разбора с документом |
| `OM-05` | Идентификатор совпал, **имена сильно расходятся** | ⚠ требует ручного разбора | Не блокирует автоматически, но требует явного решения и обоснования |
| `OM-06` | Несколько собственников / доли | Разбор по каждому | Продолжение только при полном покрытии долей |

Индикатор **никогда не сворачивается и не показывается галочкой в списке** —
это отдельный блок с рамкой, потому что это единственная защита от подставного
продавца. Имя показывается всегда во всех известных представлениях, с явной
пометкой «вторичный признак», чтобы оператор не принимал решение по имени.

### Заказ платной выписки
Кнопка показывает **стоимость до нажатия** **[установлено]**, подтверждение
расхода, отражение в учёте (`oracle:cost:expense`), запись в аудите с автором.
Состояния: заказана · получена · **ошибка получения** · реестр недоступен.
Повторный заказ — с явным предупреждением, что это повторный расход.

### Ручное подтверждение
Доступно только с приложенным документом **[установлено]**; кнопка недоступна с
причиной, пока документа нет. Обязательное обоснование, фиксация версии политики,
запись в аудит. Ручное подтверждение **не заменяет** сверку пяти полей — оно
фиксируется отдельным уровнем доверия и видно на экране утверждения выплаты как
«основание: ручное подтверждение оператора {имя}».

### Состояния экрана
`SC-empty` — наблюдений нет: что известно и что заказать.
Только L1 — явно: «этого недостаточно для выплаты», следующий шаг.
Реестр недоступен — баннер: **часы сделки приостановлены**, дедлайны сдвигаются
(`FUNCTIONAL.md` §3.5); показано, с какого момента и сколько уже длится.
Расхождение по любому из пяти полей — экран переходит в состояние «расхождение»,
выплата блокируется, создаётся задача.
`SC-denied` — роль оракула отделена от роли утверждения (см. §5.9).

### Компоненты
C-04, C-13, C-14, C-19, C-54, C-56, C-57, C-58, C-59.

---

## 5.5 Экран O-05 — Утверждение выплаты

### Назначение
**Кнопки «выплатить» не существует** **[установлено]**. Есть «утвердить»;
исполнение происходит автоматически при наборе нужного числа утверждений.
Экран показывает: **на основании чего разрешено, кто уже утвердил, чего не
хватает.**

### Данные
Транш и сделка; сумма и расщепление (получателю / комиссия / партнёр);
**пакет доказательств** со списком артефактов; порог по сумме и требуемое число
утверждений; список уже данных утверждений (кто, роль, время, устройство); список
недостающих условий; реквизиты (маскированные, статус, дата блокировки); версия
политики; состояние покрытия клиентских средств.

### Структура (`lg`)

```
┌────────────────────────────────────────────────────────────────────┐
│ H1 «ops.payout.approve.title» · {SD-7K42} · транш {1 из 1}         │
│                                                                    │
│ ▣ C-04 РАСЩЕПЛЕНИЕ                                                 │
│  «...toPayee»   {212 428 ₾}   → ▣C-38 •••• 4417 {имя} ✓ проверено │
│  «...fee»       {  1 067 ₾}   → «...operatingAccount»              │
│  «...total»     {213 495 ₾}                                        │
│  «...roundingToPayee» ⓘ  (остаток округления — получателю)         │
│                                                                    │
│ ▣ C-60 ОСНОВАНИЕ: ПАКЕТ ДОКАЗАТЕЛЬСТВ                              │
│  ✓ «...evidence.extract»       L3 · {дата} · [открыть]             │
│  ✓ «...evidence.fiveFields»    5/5 совпало · [открыть]             │
│  ✓ «...evidence.ownerMatch»    по личному номеру · [открыть]       │
│  ✓ «...evidence.kyc»           {уровень} · {дата}                  │
│  ✓ «...evidence.requisites»    проверены {дата}, заблокированы     │
│  ✓ «...evidence.testTransfer»  код подтверждён {дата}              │
│  ✗ «...evidence.coolingOff»    охлаждение до {дата} ← НЕ ЗАКРЫТО   │
│  «...policyVersion» {v12}                                          │
│                                                                    │
│ ▣ C-61 УТВЕРЖДЕНИЯ  {1 из 2}                                       │
│  ✓ {И. Иванов} · финансовый контролёр · {31.08 14:02} · {устройство}│
│  ○ «...awaiting» — требуется вторая подпись (порог {>$25k})        │
│                                                                    │
│ ▣ C-62 ЧЕГО НЕ ХВАТАЕТ                                             │
│  · «ops.payout.missing.coolingOff» {до 01.09 12:00}                │
│                                                                    │
│  [ «ops.payout.approve» ]ᵈ  ⛔ «ops.payout.blocked.coolingOff»      │
│  [ «ops.payout.reject» ]                                           │
│                                                                    │
│ «ops.payout.executionNote» — что произойдёт при наборе утверждений │
└────────────────────────────────────────────────────────────────────┘
```

### Правила экрана

- **Ни одного элемента, который выглядит как «отправить деньги».** Единственные
  действия — «утвердить» и «отклонить». Копи-бриф `...executionNote`: объяснить,
  что при наборе N утверждений поручение формируется автоматически, и что
  утверждающий не отправляет деньги вручную.
- **Пакет доказательств — это и есть основание.** Каждый артефакт открывается;
  открытие логируется. Незакрытый артефакт делает кнопку недоступной.
- Показ причины недоступности — обязателен и конкретен, а не «недостаточно прав».

### Причины блокировки утверждения (`ops.payout.blocked.*`)

| Ключ | Условие |
|---|---|
| `samePreparer` | Вы готовили эту операцию |
| `sameRequisitesEditor` | Вы заводили или меняли реквизиты по этой сделке |
| `sameOracleOperator` | Вы оператор оракула по этой сделке |
| `alreadyApproved` | Вы уже утвердили; нужна другая учётная запись |
| `noEvidence` | Пакет доказательств неполон |
| `ownerMismatch` | Собственник не совпал с профилем продавца |
| `requisitesNotVerified` | Реквизиты не прошли проверку |
| `coolingOff` | Идёт период охлаждения после изменения реквизитов |
| `blocked72h` | Реквизиты менялись в последние 72 часа |
| `coverageBreach` | Нарушено покрытие клиентских средств |
| `thresholdRole` | Ваша роль не достаточна для этого порога суммы |
| `dealFrozen` | Сделка приостановлена |
| `unknownState` | Выплата в состоянии «неизвестно» — повтор запрещён до сверки |

### Состояния экрана

| Код | Поведение |
|---|---|
| `S-01` | Готово к утверждению, вы можете утвердить |
| `S-02` | Готово, но **не вами** — причина названа; экран полностью читаем, чтобы вы могли позвать того, кто может |
| `S-03` | Не готово: список недостающего с действиями «перейти и закрыть» |
| `S-04` | Утверждено вами, ждём остальных: кто именно нужен (роль, не имя), уведомление им отправлено |
| `S-05` | Порог набран, **поручение формируется** — необратимо; экран говорит это прямо |
| `S-06` | Поручение отправлено, ждём ответ |
| `S-07` | Исполнено: дата, референс банка, проводки |
| `S-08` | Отклонено банком: причина, что дальше |
| `S-09` | **«Неизвестно»** — повтор запрещён; единственный путь — сверка; кнопка «повторить» **не существует**, есть «перейти к сверке» |
| `S-10` | Отклонено нами: причина, автор, время |
| `SC-denied` | Нет доступа к экрану утверждения вовсе (наблюдатель): видно, что операция существует |

**Состояние `S-09` — критическое.** `FUNCTIONAL.md` §2.12: переход из «неизвестно»
в повтор запрещён без сверки. В интерфейсе это означает не серую кнопку, а
**отсутствие** кнопки повтора и явный текст, объясняющий, почему её нет: повтор
без сверки — это риск двойной выплаты.

### Адаптивность
**Обязан работать на `xs`** — руководителя вызывают со второй подписью вне офиса.
На `xs`: сумма и получатель первым экраном, пакет доказательств — раскрывающийся
список, кнопка утверждения — в потоке, **не липкая** (липкая кнопка утверждения
денег провоцирует случайное нажатие). Подтверждение — отдельный экран с повтором
суммы и получателя.

### Доступность
Список утверждений — семантический список с ролями и временем; причина
недоступности связана с кнопкой через `aria-describedby`, а не только визуально
рядом; кнопка `disabled` **не используется** — используется активная кнопка,
которая при нажатии объясняет причину (disabled-элементы не получают фокус и
невидимы для скринридера) **[решение]**.

### Компоненты
C-04, C-14, C-19, C-38, C-60, C-61, C-62.

---

## 5.6 Экран O-06 — Деньги и сверка

### Назначение
**Самый верх экрана — покрытие клиентских средств** **[установлено]**. Ниже —
построчная сверка с банковской выпиской и очередь расхождений.

### Структура

```
┌────────────────────────────────────────────────────────────────────┐
│ ▣ C-63 ПОКРЫТИЕ КЛИЕНТСКИХ СРЕДСТВ            ← самый верх, всегда │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  {1,0000}   «ops.money.coverage.target» = 1                  │  │
│  │  «...balances» {4 812 300 ₾} / «...liabilities» {4 812 300 ₾}│  │
│  │  «...calculatedAt» {31.08 18:05} · «...source» {выписка+журнал}│ │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                    │
│ ▣ C-64 СВЕРКА С ВЫПИСКОЙ                                           │
│  [дата ▾] [счёт ▾]   сопоставлено {214} · расхождений {2}          │
│  ┌ выписка ──────────────┬ журнал ──────────────┬ вердикт ──────┐  │
│  │ {дата} {сумма} {назн.}│ {проводка}           │ ✓ / ⚠ / ✗     │  │
│  └───────────────────────┴──────────────────────┴───────────────┘  │
│                                                                    │
│ ▣ C-65 РАСХОЖДЕНИЯ                                                 │
│  ····{возраст 2 дня} {владелец: И.И.} {сумма} [ Разобрать ]        │
└────────────────────────────────────────────────────────────────────┘
```

### Нарушение покрытия — красный блок на весь экран **[установлено]**

```
┌════════════════════════════════════════════════════════════════════┐
║ ⚠⚠ «ops.money.breach.title»                                       ║
║                                                                    ║
║  «ops.money.breach.value»  {0,9987}                                ║
║  «ops.money.breach.gap»    {−6 240 ₾}                              ║
║  «ops.money.breach.detectedAt» {31.08 18:05}                       ║
║                                                                    ║
║  ● «ops.money.breach.intakeStopped»                                ║
║    ← состояние, а не кнопка: приём остановлен автоматически        ║
║                                                                    ║
║  «ops.money.breach.whatNow» — порядок действий                     ║
║  «ops.money.breach.notified» {кто уведомлён}                       ║
║  [ «ops.money.breach.openIncident» {INC-…} ]                       ║
└════════════════════════════════════════════════════════════════════┘
```

Правила блока:
- Занимает весь экран раздела «Деньги» и **баннером присутствует на всех экранах
  консоли**, пока отклонение не устранено.
- **Не закрывается и не сворачивается.** Кнопки «понятно» нет.
- «Приём новых сделок остановлен» — **индикатор состояния, не переключатель**.
  Возобновление приёма — отдельная процедура с двумя подтверждениями, недоступная
  с этого экрана **[решение]**: кнопка «включить обратно» рядом с описанием
  инцидента провоцирует именно то, чего нельзя делать.
- В кабинетах клиентов в это время — нейтральный баннер о временной приостановке
  приёма новых сделок, **без деталей инцидента**.

### Сверка
Построчная: строка выписки ↔ проводка. Вердикты: сопоставлено · сопоставлено с
допуском · **расхождение** · нет пары в журнале · нет пары в выписке.
Фильтр по вердикту; расхождения — вкладка по умолчанию, если они есть.
Каждое расхождение: **владелец** (кто разбирает) и **возраст** **[установлено]**;
возраст старше норматива — отдельная пометка.

### Разбор расхождения
Панель с обеими сторонами, список возможных объяснений (из перечня), обоснование,
действие (создать корректирующую проводку — **обратной проводкой, не правкой**;
запросить у банка; отнести к комиссии банка; эскалировать).
**Снятие блокировки по расхождению в одиночку невозможно** **[установлено]**:
экран показывает, что требуется второе подтверждение, и кто может его дать.

### 5.6.1 Подраздел «Непознанные поступления» (`suspense`)

Отдельная вкладка того же экрана — источник состояний `M-03` и `M-04` в кабинете
покупателя и задачи «сопоставить платёж» в очереди.

Список: дата · сумма и валюта · **имя отправителя** · назначение платежа целиком
(не обрезанное) · возраст · счёт зачисления (`suspense:unidentified`) · причина
(референс не распознан / отправитель ≠ покупатель / сумма не сходится).

Разбор одного поступления — панель, как `C-49`, но с нашими действиями:
опознать и зачислить на сделку (с обязательным основанием); признать платежом
третьего лица и оставить в удержании; вернуть отправителю (**только на
счёт-источник**, красная линия №9); запросить у клиента подтверждение перевода
(попадает в его кабинет как действие в `M-03`).

**Инварианты экрана:** деньги не зачисляются на сделку до опознания
**[установлено]**; зачисление требует основания и записи в аудит; действие
возврата недоступно без подтверждённых реквизитов отправителя из банковской
выписки. Возраст поступления виден всегда — непознанные деньги старше норматива
поднимаются в очередь работы.

Состояния: пусто (хорошее) · есть непознанные · поступление взято другим
оператором · невозможно определить отправителя (эскалация) · частичное совпадение
по сумме (предложение частичного зачисления, требует решения человека).

### Состояния
`SC-load` — покрытие показывается **последним**, только подтверждённое; до этого
скелет с явной пометкой «рассчитывается», не «1,0000» по умолчанию.
`SC-error` покрытия — **это тоже тревога**: невозможность рассчитать покрытие
трактуется как потенциальное нарушение, показывается предупреждением, а не
пустотой **[решение]**.
`SC-empty` расхождений — хорошее состояние с датой последней успешной сверки.
Выписка за день не получена — отдельное состояние, сверка невозможна, названа
причина.

### Компоненты
C-04, C-13, C-14, C-17, C-44, C-49, C-58, C-63, C-64, C-65.

---

## 5.7 Экран O-07 — Аудит

### Назначение
**Неизменяемый журнал с фильтрами, доступен наблюдателю** **[установлено]**.

### Структура
Фильтры: период, сделка, сторона, автор, тип события, роль. Таблица: время ·
автор (учётная запись + роль) · событие · объект · версия политики · ссылка на
предыдущую запись (если это исправление) · хеш.
Раскрытие записи — полный снимок данных решения.

### Поведение
- **Ни одного действия редактирования и удаления.** У записи есть только
  «внести исправление» — создаёт **новую** связанную запись **[установлено]**.
- Индикатор целостности цепочки: проверено / не проверено / **нарушено** (в
  последнем случае — тревога на весь экран, как при нарушении покрытия).
- Экспорт с фиксацией факта экспорта в самом журнале.
- Просмотр персональных данных из журнала логируется отдельно.

### Состояния
`SC-empty` фильтра — «под фильтр ничего не попало», с предложением расширить
период; отсутствие записей за период — не ошибка.
`SC-denied` — наблюдатель видит журнал, но не видит содержимое полей с
персональными данными: они помечены как скрытые, а не отсутствуют.
Большой объём — виртуализация списка, курсорная пагинация, фиксированные
заголовки.

### Компоненты
C-14, C-17, C-44, C-46, C-55.

---

## 5.8 Экран O-08 — Политики

### Назначение
**[установлено]** Пороги по суммам, правила мониторинга, тарифы — с
версионированием. Каждое решение по сделке хранит версию политики, применённую в
момент решения.

### Структура
Список политик (пороги выплат, тарифные планы, правила мониторинга, санкционные
перечни) → карточка политики → список версий → **сравнение версий**.

### Состояния версии — стейт-машина

```
черновик → на утверждении → действует → архив
   ↑            │
   └── отклонена┘
```

| Состояние | Что можно | Что видно |
|---|---|---|
| Черновик | Редактировать, отправить на утверждение | Автор, время, изменения |
| На утверждении | Утвердить (не автором), отклонить с причиной | Кто утверждает, что изменится |
| Действует | Только создать новую версию | Дата вступления; **что применяется только к сделкам после этой даты** |
| Архив | Только чтение | Период действия, какие сделки на ней |

**Пересчёт задним числом запрещён** **[установлено]** (`FUNCTIONAL.md` §4.6): при
активации новой версии интерфейс явно говорит, что она действует на будущие
сделки, и показывает количество сделок, остающихся на прежней версии.

### Сравнение версий ▣ C-66
Построчный дифф: параметр · было · стало · кто менял. Для тарифов —
дополнительно расчёт-пример на типовой сумме, чтобы изменение было видно в
деньгах, а не в процентах.

### Состояния
`SC-denied` — большинство ролей видят политики только на чтение; редактирование
доступно ограниченному кругу, недоступность объяснена.

### Компоненты
C-14, C-17, C-19, C-44, C-58, C-66.

---

## 5.9 Экран O-09 — Дежурный дашборд

### Назначение
**[установлено]** Пять метрик, на которые смотрят каждое утро.

```
┌────────────────────────────────────────────────────────────────────┐
│ ① «ops.duty.coverage»           {1,0000}   ✓                       │
│ ② «ops.duty.staleTranches»      {2}        ⚠ дольше норматива      │
│ ③ «ops.duty.unknownPayouts»     {0}        ✓ дольше суток          │
│ ④ «ops.duty.openBreaks»         {2}        ⚠ старейшее {2 дня}     │
│ ⑤ «ops.duty.dealsWithoutDeadline»{0}       ✓ (инвариант)           │
└────────────────────────────────────────────────────────────────────┘
```

Каждая метрика: значение · норматив · отклонение · **переход к списку**, который
её формирует. Метрика без перехода к списку бесполезна — по ней нельзя работать.
Метрика ⑤ — нарушение инварианта: любое значение больше нуля — тревога, не
предупреждение.

**Обязан работать на телефоне.** На `xs` — вертикальный список из пяти строк,
каждая с состоянием и переходом.

### Состояния
`SC-error` любой метрики — трактуется как отклонение, а не как «нет данных»:
невозможность посчитать «сделки без дедлайна» означает, что мы не знаем, есть ли
они. **[решение]**

### Компоненты
C-14, C-41, C-63.

---

## 5.10 Разделение ролей: как интерфейс делает нарушение невозможным

**[установлено]** Утверждение выплаты той же учётной записью, что готовила
операцию или меняла реквизиты, — запрещено. Это не настройка прав, а разные
учётные записи и разные устройства.

Как это реализовано в интерфейсе (четыре слоя, каждый обязателен):

**1. Сервер — единственный источник истины.** Экран получает `allowedActions` и
`deniedReasons`. Клиент **не вычисляет** право по роли — он его отображает.
Попытка отправить запрещённое действие отклоняется сервером и записывается в
аудит как попытка нарушения разделения обязанностей **[решение]**.

**2. Предупреждение до попытки.** На карточке сделки и на экране выплаты
постоянно видна плашка ▣ C-67: «вы участвовали в подготовке этой операции» с
перечислением, в чём именно (готовили поручение / заводили реквизиты / вели
оракул). Человек узнаёт об ограничении до того, как потратит время.

**3. Причина вместо пустоты.** Действие видно, недоступно, причина названа
конкретным ключом (§5.5). Оператор понимает систему контроля, а не борется с ней.

**4. След утверждения.** Каждое утверждение фиксирует учётную запись, роль,
время и **устройство/сессию**. Экран утверждений показывает эти данные всем
участникам процесса. Если второе утверждение приходит с той же сессии или того
же устройства, что и первое, — интерфейс показывает предупреждение, а событие
отдельно помечается в аудите **[гипотеза]**: техническая надёжность признака
«устройство» требует проверки, поэтому он используется как сигнал для аудита, а
не как жёсткая блокировка. **[открыто §8.13]**

**Чего интерфейс не делает никогда:**
- не предлагает «переключить роль», «войти как» или «временно повысить права»;
- не позволяет одному человеку держать две сессии с разными ролями в одном
  браузере — вторая сессия предупреждает и записывается **[решение]**;
- не показывает кнопку выплаты вообще, ни в каком состоянии, ни для какой роли.

---

# 6. Инвентарь общих компонентов

Компоненты — общие на три кабинета. Разница между кабинетами — **представление
по роли**, а не отдельные компоненты: два компонента «лента сделки» неизбежно
разойдутся, и стороны увидят разное о своих деньгах.

**Правило для всех без исключения:** компонент принимает ключи локализации и
данные, **никогда не содержит текстовых литералов**; денежные значения принимает
как целые минорные единицы + код валюты; даты — как момент времени + зона, не как
строку.

## 6.1 Полный инвентарь

| ID | Компонент | Назначение | Варианты | Ключевые состояния | Где |
|---|---|---|---|---|---|
| C-01 | `MoneyLocation` | «Где мои деньги» — состояния `M-01…M-18` | full, compact (липкая), inline (для консоли) | 18 состояний + load/stale/error | B-03, R-01, O-02 |
| C-02 | `PrimaryActionBlock` | Ровно одно требуемое действие или явное ожидание | action, waiting, dual (только `M-15`) | idle, busy, denied+причина, success | B-03 |
| C-03 | `DeadlineTimer` | Дедлайн + **обязательное** последствие | full, compact, inline | normal, <24ч, <1ч, наступил, приостановлен, отсутствует (баг) | все |
| C-04 | `MoneyCard` | Сумма, валюта, курс, комиссия, итог. Единый формат | single, breakdown, split (расщепление) | ready, load(скелет), stale, hidden(курс истёк) | все |
| C-05 | `SecurityBlock` | Постоянный блок безопасности | footer, inline (над реквизитами), expanded | статично + действие «сообщить» | все кабинеты, письма |
| C-05b | `PayerRuleNotice` | Правило совпадения плательщика | notice, notice+confirm | неподтверждено, подтверждено, нарушено | B-04 |
| C-06 | `DealTimeline` | Лента сделки, **три представления по роли** | buyer, payee, ops | пройден, текущий, предстоящий, ветка отката, пауза часов | B-03, R-01, O-02 |
| C-07 | `PropertyCard` | Объект: реестр против договора | summary, comparison | ok, расхождение (блокирует), данные устарели, нет данных | B-03, R-01, O-02 |
| C-08 | `AssuranceLevel` | Лестница `A0…A4` подтверждения средств | bar, compact | 5 уровней + понижение | R-01 |
| C-09 | `CoverageMeter` | Покрытие источника средств (≥90%) | client, ops | ниже порога, порог пройден, нет данных | B-02, O-03 |
| C-10 | `PartiesAndDocs` | Стороны с уровнем проверки + документы | buyer, payee, ops | — | B-03, R-01 |
| C-11 | `FxCalculator` | Двунаправленный расчёт валюты | — | ввод, пересчёт, курс недоступен, вне лимитов | B-04 |
| C-12 | `RateLockTimer` | Фиксация курса `Q-01…Q-07` | inline, banner, blocking | 7 состояний | B-04, B-03 |
| C-13 | `GlobalBanner` | Стек глобальных предупреждений (макс. 2) | info, warning, critical | — | все |
| C-14 | `ErrorState` | Ошибка: что, деньги в безопасности, что делать, номер | page, block, inline, toast | сеть, сервер, права, не найдено, устарело | все |
| C-15 | `CreditingStatus` | Статус зачисления перевода | — | не заявлен, ждём, задержка, не опознан, зачислен, возвращён | B-04 |
| C-16 | `RequisitesList` | Реквизиты с копированием и референсом | payin (с референсом), payout (маскированные) | ready, load, **никогда не из кэша** | B-04, R-02 |
| C-17 | `RecordList` | Список записей: движения, история, сессии, лог | list (xs), table (md+) | ready, empty, load, error, частичная загрузка | все |
| C-18 | `DocumentViewer` | Просмотр с водяным знаком и логированием | inline, fullscreen | load, ready, не поддерживается, повреждён, нет прав | B-06, R-03, O-* |
| C-19 | `StatusBadge` | Статус: **цвет + форма + текст** | neutral, ok, warn, danger, progress | — | все |
| C-20 | `AppHeader` | Шапка, идентификация кабинета, язык | buyer, payee, ops | — | все |
| C-21 | `LanguageSwitcher` | RU / EN / KA (консоль — RU / EN) | inline, menu | — | все, включая экран входа |
| C-22 | `BottomNav` | Нижняя навигация покупателя (`xs`–`sm`) | — | активный, бэдж | B-* |
| C-23 | `OtpInput` | Одноразовый код | 6 ячеек | ввод, ошибка, попытки исчерпаны, автоподстановка | B-01, R-* |
| C-24 | `Field` | Поле формы: метка сверху, подсказка, ошибка | text, number, money, iban, select, date, textarea | default, focus, error, disabled, readonly+причина | все формы |
| C-25 | `Button` | Действие | primary, secondary, ghost, danger | idle, busy, denied+причина, success. **Ширина по контенту, 2 строки допустимы** | все |
| C-26 | `StepIndicator` | Прогресс мастера с подписями | horizontal (xs), vertical (lg) | пройден, текущий, предстоящий, с ошибкой | B-02, R-11 |
| C-27 | `ChoiceList` | Выбор с описаниями-примерами | single, multi | выбрано, недоступно+причина, предупреждающий вариант | B-02 |
| C-28 | `DocumentRequirement` | Требование к документу: что это, что видно, пример, статус | required, optional | не приложено, загрузка, на проверке, принято, заменить+причина | B-02, R-03 |
| C-29 | `ConsentCheckbox` ⚖ | Отдельное согласие со ссылкой и версией | — | не дано, дано (с фиксацией версии) | B-02 |
| C-30 | `DealSwitcher` | Переключатель сделок; при одной — не рендерится | — | — | B-03, R-01 |
| C-31 | `MethodSelector` | Способ пополнения | — | доступен, недоступен+причина (карта выше порога) | B-04 |
| C-32 | `FileUpload` | Загрузка с проверкой качества | single, multi | idle, upload+прогресс, ошибка формата/размера, успех, прервано | B-02, B-04, O-* |
| C-33 | `DocumentExample` | Образец документа в потоке, не модально | — | свёрнут, раскрыт | B-02 |
| C-34 | `WithdrawForm` | Вывод только на счёт-источник | — | `W-01…W-07` | B-05 |
| C-35 | `SupportRequestForm` | Обращение / сообщение о подозрении | report, generic | отправка, принято+номер, ошибка с сохранением текста | B-07, R-04 |
| C-36 | `SideNav` | Боковая навигация со счётчиками | payee, ops | активный, счётчик, недоступный раздел+причина | R-*, O-* |
| C-37 | `FundsAssuranceCard` | Карточка подтверждения средств | full, preview | `S-01…S-14` (§4.1) | R-01 |
| C-38 | `MaskedAccount` | Счёт: маска, имя владельца, статус проверки | inline, block | проверен, не проверен, заблокирован, охлаждение | R-01, R-02, O-05 |
| C-39 | `PerimeterState` | Состояние периметра реквизитов | — | `P-01…P-12` (§4.2) | R-02 |
| C-40 | `TestTransferVerify` | Тестовый перевод с кодом | — | 6 состояний (§4.2) | R-02 |
| C-41 | `MetricTile` | Метрика: значение, норматив, отклонение, переход | — | ok, warn, critical, нет данных = тревога | R-10, O-09 |
| C-42 | `UnmatchedQueue` | Очередь несопоставленных, **на виду** | dashboard, full | пусто = хорошее состояние | R-10, R-12 |
| C-43 | `PaymentScheduleList` | Ближайшие и просроченные платежи | upcoming, overdue | — | R-10, R-11 |
| C-44 | `DataTable` | Таблица: сортировка, приоритет+, липкие заголовки | — | ready, empty, empty-фильтра, load, error, частичная | R-*, O-* |
| C-45 | `ImportWizard` | Импорт таблицей с построчной проверкой | — | 5 шагов + частичный успех с выгрузкой отклонённых | R-11 |
| C-46 | `FilterBar` | Чипы и фильтры, перенос на новую строку | chips, panel (xs) | активные фильтры видны, сброс | R-*, O-* |
| C-47 | `EmptyState` | Пустое состояние: почему пусто и что появится | neutral, positive (пустая очередь), filtered | — | все |
| C-48 | `MatchConfidence` | Уверенность сопоставления + **почему** | — | высокая, средняя, низкая, нет кандидатов | R-12 |
| C-49 | `MatchPanel` | Разбор несопоставленного платежа | — | кандидаты, поиск, предложено, занято другим | R-12 |
| C-50 | `BackgroundJob` | Длинная операция: уйти со страницы, уведомление | — | ставится, идёт, готово, ошибка, пустой результат | R-14, O-* |
| C-51 | `TaskCard` | Карточка задачи: тип, сделка, сумма, дедлайн, одно действие | — | свободна, моя, взята другим, просрочена, заблокирована | O-01 |
| C-52 | `TaskTypeBadge` | Тип задачи (8 типов) | — | — | O-01 |
| C-53 | `SectionNav` | Навигация по разделам карточки сделки | vertical (xl), tabs (lg) | активный, недоступный+причина | O-02 |
| C-54 | `ObservationList` | Наблюдения оракула с **уровнем доверия L1–L5** | — | нет наблюдений, только L1 (недостаточно), L3+, ошибка получения | O-04, O-02 |
| C-55 | `AuditLog` | Неизменяемый журнал | full, embedded | целостность: проверена / не проверена / **нарушена** | O-07, O-02 |
| C-56 | `FieldComparison` | Сравнение полей: ожидалось / фактически / вердикт | oracle (5 полей), sanctions, property | совпало, разошлось, нет данных | O-02, O-03, O-04 |
| C-57 | `OwnerMatchIndicator` | **Совпадение собственника с профилем продавца** | — | `OM-01…OM-06` (§5.4) | O-04, O-05 |
| C-58 | `DecisionForm` | Решение с основанием из перечня + обоснование + версия политики | — | не заполнено, отправка, принято, ошибка | O-03, O-04, O-06 |
| C-59 | `ThresholdBand` | Порог по сумме и требования к нему | — | выполнено / не выполнено по пунктам | O-04, O-05 |
| C-60 | `EvidencePack` | **Пакет доказательств** — основание выплаты | — | полон, неполон (с указанием чего), артефакт недоступен | O-05, O-02 |
| C-61 | `ApprovalList` | Кто утвердил: учётная запись, роль, время, устройство | — | набрано N из M, ждём роль X, одна сессия (предупреждение) | O-05 |
| C-62 | `BlockingChecklist` | Чего не хватает, с переходом к закрытию пункта | — | — | O-05 |
| C-63 | `CoverageIndicator` | **Покрытие клиентских средств** | header, fullscreen-breach | = 1 (ок), ≠ 1 (**красный блок на весь экран**), не рассчитано = тревога | O-06, O-09, баннер везде |
| C-64 | `ReconciliationTable` | Построчная сверка с выпиской | — | сопоставлено, с допуском, расхождение, нет пары | O-06 |
| C-65 | `BreaksQueue` | Расхождения с владельцем и возрастом | — | пусто = хорошее состояние, старше норматива | O-06 |
| C-66 | `VersionDiff` | Сравнение версий политики, с примером в деньгах | — | — | O-08 |
| C-67 | `InvolvementNotice` | «Вы участвовали в подготовке этой операции» | — | подготовил, менял реквизиты, вёл оракул | O-02, O-05 |

## 6.2 Детальные спеки критических компонентов

### C-01 `MoneyLocation`

**Когда использовать:** единственный компонент, отвечающий на вопрос «где деньги».
**Когда не использовать:** для статуса сделки — это `C-19`; для суммы — `C-04`.

**Анатомия:** индикатор состояния (форма + цвет + текст) · заголовок (H2) · тело
1–2 фразы · сумма `C-04` · одна вспомогательная ссылка (необязательно).

**Свойства:** `state: M-01…M-18` · `role: buyer|payee|ops` · `amount:
{minorUnits, currency}` · `deadline?: Deadline` · `variant: full|compact|inline` ·
`modifiers: clockPaused|quoteExpired|coverageBreach|...`

**Инварианты:**
- Одно состояние в один момент. Комбинация «загрузка + данные» невозможна.
- `role` меняет **ключи**, но не состояние: покупатель и продавец видят разные
  формулировки одного и того же факта, и это гарантирует, что они не увидят
  противоречивого.
- Сумма в `SC-load` — скелет, не ноль. В `SC-stale` — помечена.
- В `M-04`, `M-18` сумма помечена как недоступная, а не показана как баланс.

**Доступность:** `role="status"` с `aria-live="polite"`; смена состояния
объявляется одной фразой (заголовок), не всем содержимым; индикатор — не только
цвет.

**Нельзя:** сворачивать, скрывать за вкладкой, анимировать сумму, показывать
«примерно», выводить за первый экран на `xs`.

---

### C-03 `DeadlineTimer`

**Инвариант:** не рендерится без `consequenceKey`. Дедлайн без последствия —
ошибка типа, а не решение дизайнера.

**Анатомия:** абсолютное время в операционной зоне (Asia/Tbilisi) с меткой зоны ·
то же в локальной зоне пользователя, если отличается · относительный остаток ·
последствие.

**Состояния:** `normal` · `soon` (<24 ч) · `imminent` (<1 ч) · `reached` (срок
наступил, идёт обработка — **не «просрочено»**) · `paused` (часы остановлены,
причина, «срок будет пересчитан») · `absent` (нарушение инварианта: показывает
предупреждение и создаёт обращение).

**Доступность:** обновление не чаще раза в минуту в `aria-live="polite"`; за 10
минут — одно `assertive`; относительное время **никогда не единственное**.

**Нельзя:** показывать только относительное время; мигать; скрывать последствие
под раскрытием; показывать «00:00:00» после наступления вместо состояния
`reached`.

---

### C-04 `MoneyCard`

**Инварианты:**
- Принимает **целые минорные единицы**; тип запрещает `number` с плавающей точкой.
- Форматирование — `Intl.NumberFormat` по локали пользователя, валюта всегда
  рядом с суммой.
- Курс — всегда с направлением («1 USD = X ₾»), никогда голое число.
- Одна и та же сумма в двух кабинетах рендерится одним компонентом с одними
  данными: **расхождение цифр между экранами — блокирующий дефект** (A2 в
  `PRODUCT.md` §13).
- При `quoteExpired` суммы в лари **скрыты**, а не устаревшие.
- Комиссия — отдельной строкой с раскрытием состава (**[открыто §8.1]**).

**Варианты:** `single` (одна сумма) · `breakdown` (сумма + вычеты + итог) ·
`split` (расщепление: получателю / комиссия / партнёр, с пометкой, что остаток
округления — получателю).

**Нельзя:** обрезать, сокращать разряды («213,5 тыс.»), показывать без валюты,
считать на клиенте.

---

### C-05 `SecurityBlock`

**Содержание [установлено]:** мы не сообщаем реквизиты по телефону, почте, в
мессенджере; единственное место, где вы видите реквизиты — этот кабинет; мы
никогда не просим перевести деньги на другой счёт. Плюс действие «сообщить о
подозрительном контакте».

**Варианты:** `footer` (по умолчанию) · `inline` (над реквизитами, усиленный) ·
`expanded` (экран безопасности).

**Нельзя:** сворачивать, делать закрываемым, показывать как модальное окно,
опускать в письмах, показывать разное содержание в разных кабинетах.

---

### C-06 `DealTimeline`

**Один компонент, три представления** **[установлено]**. Представление меняет
только набор ключей и уровень детализации:
`buyer` — человеческие формулировки; `payee` — то же, со своей стороны;
`ops` — технические состояния с временными метками и авторами переходов.

**Инвариант:** порядок и количество шагов у покупателя и продавца **совпадают**.
Стороны не должны видеть разное число шагов — иначе разговор между ними
невозможен.

**Ветка отката** — не ошибка, а альтернативная ветвь: шаги «регистрация» и
«расчёт» помечаются непройденными, добавляются шаги отката.
**Пауза часов** — вставка в ленту с причиной и временем.

---

### C-19 `StatusBadge`

**Инвариант доступности:** статус передаётся **тремя** признаками — текст, цвет,
форма индикатора. Ни один статус не различим только по цвету.
Текст всегда виден; бэдж без текста (только точка) запрещён.
Ширина по контенту: грузинский статус длиннее английского в полтора раза.

---

### C-37 `FundsAssuranceCard`

**Когда использовать:** главный экран продавца, единственное место.
**Не использовать** как превью в списке — для этого `preview`, где не показываются
исходы (усечённое обещание хуже отсутствующего).

**Обязательные элементы, ни один не опционален:** сумма · момент резервирования ·
банк-держатель и тип счёта · **оба исхода, равные по визуальному весу** · сумма к
получению с вычетами · реквизиты и статус их проверки · дедлайн с последствием ·
граница обещания ⚖ · номер подтверждения и способ проверки.

**Правило симметрии:** блок «регистрация не состоялась» не может быть визуально
слабее блока «регистрация подтверждена». Проверяется на макете линейкой: равная
площадь, равный размер шрифта, равная позиция в иерархии.

**Нельзя:** называть гарантией; обещать необратимость; скрывать плохой исход под
раскрытием; показывать без номера подтверждения.

---

### C-51 `TaskCard`

**Анатомия:** тип `C-52` · идентификатор сделки и объект · сумма `C-04` ·
дедлайн `C-03` с последствием · возраст · владелец · **одно** основное действие.

**Состояния:** свободна · моя · взята другим (с указанием кто и когда) ·
просрочена (поднимается, меняет вид, **не исчезает**) · заблокирована (причина).

**Нельзя:** добавлять второе основное действие; делать всю карточку одной
кнопкой; скрывать просроченные; массовые операции.

---

### C-57 `OwnerMatchIndicator`

**Назначение:** единственная защита от подставного продавца.

**Инварианты:**
- Первичный признак — **личный номер или номер документа**. Имя — вторичный,
  показывается с явной пометкой **[установлено]**.
- Имя показывается во всех известных представлениях (реестр на грузинском,
  документ на латинице, каноническое) с коэффициентом схожести.
- `OM-03` (идентификатор не совпал) — **жёсткий стоп**, блокирует выплату
  полностью, без возможности перекрыть решением оператора.
- Не сворачивается, не показывается галочкой в общем списке.

**Нельзя:** показывать только процент схожести имени; трактовать высокое
совпадение имени как подтверждение; скрывать `OM-04` (нет идентификатора) как
«нет данных» — это блокирующее состояние.

---

### C-60 `EvidencePack` + C-61 `ApprovalList`

**C-60 инварианты:**
- Список артефактов, каждый — с уровнем/датой/автором и ссылкой на просмотр.
- Открытие артефакта логируется.
- Неполный пакет делает утверждение недоступным; **чего не хватает — названо**.
- Пакет виден целиком до утверждения: утверждающий не должен искать основание.

**C-61 инварианты:**
- Показывает учётную запись, роль, время, устройство/сессию каждого утверждения.
- Показывает, **какой роли не хватает**, а не «нужна ещё одна подпись».
- Совпадение сессии/устройства у двух утверждений — предупреждение + пометка в
  аудите **[гипотеза, §8.13]**.
- Не содержит и **не может содержать** действие «выплатить».

---

### C-63 `CoverageIndicator`

**Два режима:**
`header` — значение, цель (= 1), время расчёта, источник. Всегда на самом верху
экрана «Деньги»; в свёрнутом виде — в глобальном баннере на всех экранах консоли.
`fullscreen-breach` — **красный блок на весь экран** при любом отклонении
**[установлено]**: значение, разница в минорных единицах, время обнаружения,
статус «приём новых сделок остановлен» (**индикатор, не переключатель**), порядок
действий, кто уведомлён, ссылка на инцидент.

**Инварианты:**
- Не закрывается и не сворачивается, пока отклонение не устранено.
- Возобновление приёма недоступно с этого экрана **[решение]**.
- Состояние «не рассчитано» трактуется как отклонение, не как «нет данных».
- В клиентских кабинетах — нейтральный баннер о приостановке приёма **без
  деталей инцидента**.

---

# 7. Чек-лист приёмки

Проверяется на каждом экране в каждом состоянии, на грузинском языке, на 360 px и
при зуме 200%.

### 7.1 Три вопроса
- [ ] Где деньги — виден ответ, включая состояния загрузки, ошибки и запрета.
- [ ] Что дальше — названо событие и срок, включая «что будет, если ничего не
      делать».
- [ ] Что от меня — ровно одно действие или явное ожидание с указанием субъекта.

### 7.2 Состояния
- [ ] `SC-load`, `SC-empty`, `SC-partial`, `SC-stale`, `SC-error`, `SC-denied`,
      `SC-offline`, `SC-frozen` — специфицированы, не «по остаточному принципу».
- [ ] Пустое состояние отличается от результата фильтра.
- [ ] Пустая очередь работы и пустая очередь расхождений выглядят как хорошее
      состояние, а не как поломка.
- [ ] Ошибка сохраняет введённые данные.
- [ ] Ни одна сумма не показана из кэша без пометки; реквизиты не показаны из
      кэша никогда.

### 7.3 Деньги и сроки
- [ ] Все суммы через `C-04`, из минорных единиц, с валютой.
- [ ] Одна сумма одинакова во всех кабинетах (критерий A2).
- [ ] Каждый дедлайн имеет последствие и две зоны.
- [ ] Ни одного нетерминального транша без видимого дедлайна.
- [ ] Курс с направлением и сроком фиксации; истёкший курс не показывается как
      действующий.

### 7.4 Локализация
- [ ] Ни одного текстового литерала в компонентах (линтер).
- [ ] Макет проверен на грузинском и на псевдолокали +60%.
- [ ] Кнопки, чипы и бэджи не обрезаются, переносятся на две строки.
- [ ] Суммы, даты и статусы в таблицах не обрезаются.
- [ ] Плюрализация через ICU, без конкатенации.
- [ ] Логические свойства вместо физических (задел под RTL).

### 7.5 Безопасность
- [ ] Блок безопасности присутствует на каждом экране и в каждом письме.
- [ ] Правило плательщика показано **до** перевода.
- [ ] Реквизиты выплаты недостижимы без свежего второго фактора.
- [ ] Имя владельца счёта нередактируемо.
- [ ] Ни одной ссылки на действие в письмах.
- [ ] Ни одной кнопки «выплатить» нигде в интерфейсе.
- [ ] Запрещённые действия видны с причиной; право проверяется сервером.

### 7.6 Доступность (WCAG 2.1 AA)
- [ ] Контраст ≥ 4.5:1 для текста, ≥ 3:1 для элементов управления и индикаторов.
- [ ] Статус не передаётся только цветом (проверено симуляцией дальтонизма).
- [ ] Сенсорные цели ≥ 44×44 px, включая иконки копирования.
- [ ] Полная работа с клавиатуры; видимый фокус; логический порядок.
- [ ] Ошибки связаны с полями; сводка ошибок фокусируема.
- [ ] Таймеры не «кричат» в `aria-live`; ограничение по времени продлеваемо и не
      теряет данные (2.2.1).
- [ ] `disabled` не используется там, где нужна причина (§5.5).
- [ ] Зум 200% и системный шрифт +30% не ломают ни один экран.
- [ ] `prefers-reduced-motion` уважается.
- [ ] Альтернатива биометрии доступна и не требует голоса или зрения как
      единственного канала (**[открыто §8.14]**).

---

# 8. Открытые вопросы к владельцу

Пункты 1–3 — из `CABINETS.md` §8, повторены здесь, потому что от них зависят уже
спроектированные экраны. Остальные возникли при проектировании.

**1. Комиссия платформы покупателю — отдельной строкой или только итог?**
Спроектировано **с раскрытием** и ссылкой «из чего состоит» (B-03 блок 5, B-04).
За: потребительское право, честность, комиссия и спред — разные вещи с разным
НДС. Против: делает нас сравнимыми по ставке с госсервисом за 35 лари.
*Цена смены решения: низкая — один блок и ключи.*

**2. Есть ли кабинет до заведения сделки оператором?**
Спроектировано в предположении «кабинет появляется вместе со сделкой»; заготовка
`SC-empty` для обратного варианта описана в §3.3.
*Цена смены решения: средняя — появляется список сделок и экран онбординга.*

**3. Курс — фиксация на N часов с таймером или плавающий до зачисления?**
Спроектирована **фиксация с таймером** (`C-12`, состояния `Q-01…Q-07`), включая
блокирующий пересчёт `Q-07`, если деньги пришли после истечения окна. Нужны:
длительность окна N и правило поведения при `Q-06` (котировка недоступна).
*Цена смены решения: высокая — меняется весь экран пополнения.*

**4. Что видит покупатель при неопознанном поступлении (`M-03`)?**
Спроектировано **честно**: «поступление получено, но не опознано, на сделку не
зачислено». Альтернатива — не показывать до опознания. Первое снимает панику
«деньги ушли и пропали», второе не раскрывает внутреннюю кухню и не создаёт
ложных ожиданий, если поступление вообще чужое.

**5. Показывать ли покупателю процент покрытия источника средств?**
Спроектировано: показываем порог и прогресс (B-02, шаг 5.2). За: превращает
непонятную бюрократию в решаемую задачу и снижает число циклов. Против:
подсказывает, как подогнать документы под порог. Компромисс в спеке — показываем
порог, не показываем сработавшие детекторы.

**6. Какие языки документов мы принимаем без перевода?**
Влияет на текст требований (B-02, шаг 5.3) и на число возвратов. Нужен перечень
до реализации, потому что он называется клиенту **заранее**, а не после отказа.

**7. Поведение при недоступности котировки (`Q-06`).**
Принимать деньги без зафиксированного курса (конвертация позже, с правом клиента
отказаться) или блокировать пополнение? Первое сохраняет поток, но создаёт
курсовой риск и потенциальную претензию; второе останавливает продажи на время
инцидента у поставщика котировок.

**8. Отправка реквизитов на почту.**
Нужна сегменту, который платит из отделения банка. Но противоречит формулировке
«единственное место, где вы видите реквизиты — этот кабинет». Варианты: не
отправлять вовсе; отправлять PDF без ссылок с явной оговоркой; отправлять только
уведомление «реквизиты в кабинете». *Спроектировано с оговоркой, требует решения.*

**9. Видит ли клиент, что его документ смотрел кто-то ещё?**
Журнал доступа ведётся всегда. Вопрос — показывать ли клиенту чужие просмотры.
За: доверие и симметрия. Против: раскрывает нашу внутреннюю работу и создаёт
вопросы «почему смотрели пять раз».

**10. Параметры тестового перевода (R-02).**
Сумма, за чей счёт, возвращается ли, срок жизни кода, число попыток (в спеке
принято 3). Влияет на текст и на учёт.

**11. В каком формате застройщик принимает отчёты?**
«Формат, который принимает его учётная система» — нужен конкретный перечень до
реализации R-14. Иначе выгрузка будет нашей внутренней, а не его.

**12. Сопоставление платежа застройщиком — решение или предложение?**
Спроектировано как **предложение** с последующим подтверждением у нас (веб-слой
не двигает деньги). Альтернатива — автоматическое применение при высокой
уверенности. Влияет на A3 (≥85% автосверки) и на нагрузку операторов.

**13. Использовать ли устройство/сессию как признак разделения обязанностей?**
`CABINETS.md` §1 говорит «разные учётные записи и разные устройства». Надёжность
признака «устройство» в вебе — **[гипотеза]**. Спроектировано как **сигнал для
аудита и предупреждение**, не как жёсткая блокировка. Нужно решение: достаточно
ли этого, или требуется отдельный контур (например, утверждение только из
корпоративной сети).

**14. Третий путь вместо биометрии для человека, который не может пройти
видеозвонок.**
Требование доступности: видеозвонок предполагает зрение, слух и речь. Нужен
офлайн-путь (визит, доверенное лицо) — есть ли он на пилоте и как оформляется.

**15. Язык консоли операций.**
`FUNCTIONAL.md` §5.1 — русский и английский. Если операторы грузиноязычные,
решение стоит принять до первого релиза консоли: добавить третий язык дешевле
сразу, чем после сотни экранов.

**16. Часовой пояс дедлайна для покупателя.**
Спроектировано: две зоны всегда (Тбилиси + локальная). Нужно подтверждение, что
операционная зона — именно Asia/Tbilisi и что отсечка «конец банковского дня»
фиксирована (18:00) или зависит от банка.

**17. Раскрытие правил распределения расходов при срыве сделки** ⚖.
`FUNCTIONAL.md` §4.4 требует показывать их сторонам «в реальном времени». Где
именно: отдельным блоком на экране сделки, в документах, или строкой в блоке
денег? Спроектировано как строка в состояниях отката + ссылка на полный текст;
формулировка идёт через `legal-compliance-ru`, не через главреда.

---

## Что дальше

1. Решения по §8 — блокируют финализацию экранов B-04, R-02, R-14, O-05.
2. Копи-брифы этого документа уходят копирайтеру, оттуда — главреду; слоты ⚖ —
   в `legal-compliance-ru`.
3. После принятия ключей — дизайн-система: токены, типографика, цвет, иконки,
   сетка. Инвентарь §6 — её вход.
4. Прототип двух экранов на грузинском для проверки бюджетов длины (§1.1) до
   верстки остальных: **B-03** (самый плотный клиентский) и **O-05** (самый
   плотный операторский).
