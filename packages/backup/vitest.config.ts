import { defineConfig } from 'vitest/config';

/**
 * Офлайн-набор: ни Postgres, ни `pg_dump`, ни файлов копии.
 *
 * Всё, что можно проверить без базы, вынесено в чистые функции — разбор строки
 * подключения, вырезание пароля, сверка переписи, признаки нездорового
 * состояния. Корневой `pnpm test` обязан оставаться бессетевым (`CLAUDE.md`),
 * поэтому учения по восстановлению живут в `test:int` и запускаются явно.
 */
export default defineConfig({
  test: {
    include: ['test/*.test.ts'],
  },
});
