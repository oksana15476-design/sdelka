/**
 * `@sdelka/pricing` — **величины, которыми владелец управляет заработком**:
 * тариф, плательщик комиссии и наценка к курсу (E16-4, E16-5, E16-11).
 *
 * Что здесь есть: тарифный план и расписание наценок как **значения версий
 * настройки**, границы, которые отвергают негодную величину на входе,
 * тарификация транша по версии, действовавшей в момент его создания, и
 * клиентский курс, выведенный из эталонного целочисленно.
 *
 * Чего здесь нет: второго механизма версионирования. Версия, журнал версий,
 * порядок записи, запрет «задним числом» и резолвер действующей версии живут в
 * `@sdelka/settings` и отсюда только используются. Пакет настроек не знает ни
 * одной величины намеренно — величину подключает тот, кому она принадлежит, и
 * это он.
 *
 * Чего здесь нет и не появится без ответа владельца: **самих чисел**. Ставка,
 * потолок наценки и плательщик по умолчанию помечены `PROVISIONAL_` и вынесены
 * в `docs/product/DECISIONS-REVIEW.md` §J с пометкой **[открыто]**.
 */
export * from './errors';
export * from './keys';
export * from './payer';
export * from './quotation';
export * from './rate';
export * from './series';
export * from './spread';
export * from './tariff';
