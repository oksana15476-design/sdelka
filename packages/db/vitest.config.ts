import { defineConfig } from 'vitest/config';

/**
 * Офлайн-набор: сеть не нужна, Postgres не нужен.
 *
 * `pnpm test` в корне запускает `test` каждого пакета, и если бы здесь стоял
 * интеграционный набор, корневой прогон перестал бы быть бессетевым и падал бы
 * у любого, у кого кластер не поднят. `CLAUDE.md`: «тесты стейт-машины и учёта
 * прогоняются без сети». Интеграция — отдельной командой `test:int`.
 */
export default defineConfig({
  test: {
    include: ['test/*.test.ts'],
  },
});
