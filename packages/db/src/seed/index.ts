/**
 * Засев базы сценариями интерфейса.
 *
 * ⚠ **Из общей точки входа пакета (`src/index.ts`) этот каталог намеренно не
 * вывозится.** Он зависит от `@sdelka/app` — слоя приложения со всем его
 * графом, — а `@sdelka/db` импортируют и миграции, и `apps/web`. Барель,
 * тянущий засев, затащил бы слой приложения в сборку каждого из них ради
 * команды, которая запускается вручную. Кому засев нужен, тот берёт его
 * отсюда — командой `pnpm --filter @sdelka/db seed` либо прямым импортом.
 */
export * from './errors.ts';
export * from './guard.ts';
export * from './run.ts';
export * from './verify.ts';
