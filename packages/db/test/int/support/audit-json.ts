/**
 * Кодировка тела записи аудита переехала в `src/store/audit-json.ts`.
 *
 * Здесь она стояла с оговоркой «как именно приложение положит тело в `jsonb` —
 * его решение». Приложение появилось (`src/store/audit.ts`), решение принято, и
 * вторая кодировка в тестах означала бы, что набор проверяет собственный круг, а
 * не тот, которым пользуется продукт. Реэкспорт оставлен, чтобы существующие
 * проверки продолжали смотреть на ту же функцию, но уже настоящую.
 */
export { fromJson, toJson } from '../../../src/store/audit-json.ts';
