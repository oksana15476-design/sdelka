/**
 * `@sdelka/limits` — **периметр приёма как настройка**: перечень валют сделки
 * (E16-6) и пороги — допуск по сумме поступления, порог значимости, возрастные
 * границы очереди разбора (E16-7).
 *
 * Что здесь есть: величины и их границы. Перечень валют, который нельзя
 * оставить без лари и нельзя включить без пяти предпосылок; пороги, которые
 * отвергаются **на записи настройки**, а не при применении; привязка каждой
 * величины к своему моменту; запись изменения в журнал аудита (E16-12).
 *
 * Чего здесь нет: второго механизма версионирования. Версия, журнал версий,
 * порядок записи, запрет «задним числом» и резолвер действующей версии живут в
 * `@sdelka/settings` и отсюда только используются. Пакет настроек не знает ни
 * одной величины намеренно — величину подключает тот, кому она принадлежит.
 *
 * Чего здесь нет и не появится без ответа владельца: **самих чисел**. Все
 * значения помечены `PROVISIONAL_`, перенесены из мест, где они лежат
 * константами сегодня, и вынесены в `docs/product/DECISIONS-REVIEW.md` §K с
 * пометкой **[открыто]**.
 *
 * Чего здесь нет по устройству: применения величин. Допуск считает
 * `@sdelka/intake`, уровень эскалации — `@sdelka/compliance`, покрытие по
 * валютам — `@sdelka/ledger`. Формы значений совпадают с тамошними поле в поле,
 * чтобы версия подставлялась туда как есть и вторая редакция расчёта не
 * заводилась ни одна.
 */
export * from './audit';
export * from './currencies';
export * from './errors';
export * from './keys';
export * from './series';
export * from './thresholds';
