/**
 * `@sdelka/settings` — **форма настройки, а не её величины**.
 *
 * Что здесь есть: версия значения с автором, основанием, моментом записи и
 * моментом вступления в силу; журнал версий, дописываемый только в конец;
 * резолвер действующей версии на названный момент; прилипание, выраженное
 * типом; храповик как отдельная стратегия-исключение.
 *
 * Чего здесь нет и не появится до ответов владельца (`SETTINGS.md` §10): ни
 * одной величины. Ни ставки, ни наценки, ни валют, ни порогов, ни окон, ни
 * одного числа. Форма и журнал одинаковы при любом ответе — потому их можно
 * строить сейчас; величины подключит пакет, которому они принадлежат.
 *
 * `effective.ts` из индекса не выведен намеренно: см. комментарий в нём.
 */
export * from './attachment';
export * from './errors';
export * from './keys';
export * from './ratchet';
export * from './resolve';
export * from './series';
export * from './version';
