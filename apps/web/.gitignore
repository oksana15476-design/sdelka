# Скриншоты — воспроизводимый артефакт: их целиком пересобирает
# `pnpm --filter @sdelka/web verify:ui`. В репозиторий не кладём (28 МБ на
# прогон), но и не прячем: путь и имена файлов детерминированы, прогон
# повторяется одной командой.
screenshots/
